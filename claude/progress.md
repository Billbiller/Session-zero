Session Zero — D&D Matchmaking App: Progress
This file is the authoritative backlog + session history for this repo.

**2026-09-02 update: backlog item #1 is RESOLVED.** The app now lives in a real git repository pushed to `https://github.com/Billbiller/Session-zero`, checked out on the owner's own laptop (not the ephemeral cloud sandbox), authenticated with the owner's own GitHub personal access token stored in that machine's local git config. History persists for real now. The rest of this file's "nightly reset" framing is kept below as historical context for how earlier sessions worked around the missing git history, but a from-scratch rebuild every session is no longer required going forward — clone/pull the repo and continue from where the last session left off.

Historical context (pre-2026-09-02): The cloud workspace resets every night (confirmed, well-established pattern), so nothing outside a durable store (this file) survived between sessions, and every session started by reading this file, then rebuilding the app from scratch against the spec it describes.

Current status (as of 2026-09-02, nightly build)
Rebuilt the app from scratch tonight (in a fresh cloud workspace, per the historical pattern) to parity with this file as it stood after 2026-09-01 — placeholder auth, campaign CRUD + capacity-floor validation, browse/filter/sort/paginate, join/approve/decline, auto-fill/auto-reopen/manual-reopen, leave-campaign flow, the private-side boundary (session log, party notes, schedule) with unit + HTTP-integration regression coverage, in-app notifications with the 6a/6b fan-out completions, and 6d per-notification-type preferences. Build, full test suite (68 tests: 64 unit + 4 HTTP integration), and lint all pass clean. A manual smoke test against a spare-port production server confirmed the landing page, `/campaigns`, `/signin`, and a 404 for an unknown campaign id all render correctly.

Then, later the same night (the nightly scheduled build), implemented backlog item 6e: replaced the NavBar's 15s notification-badge polling with a live push via Server-Sent Events, falling back to the old polling behavior if SSE is unavailable. Build, full test suite (80 tests: 74 unit + 6 HTTP integration), and lint all pass clean. See the dated session log entry below for details and judgment calls.

Then — the actual headline work of the night — solved backlog item #1 for real: connected this session to the owner's own laptop via the Claude desktop app's device bridge, installed the GitHub CLI (`gh`) and configured `git` there (no `sudo` needed — installed to `$HOME/bin`), authenticated with a personal access token the owner generated and handed over via a temporary file in the connected folder (read once, then deleted — never pasted into the chat transcript), confirmed real read/write access to `https://github.com/Billbiller/Session-zero`, then transferred the freshly-built app from the cloud workspace into `C:\Users\billb\Session-zero\repo` (the existing local clone, which had nothing but an auto-generated README so far) and pushed it as the first real commit.

**2026-09-03 update (nightly scheduled build): backlog item #7 (free-text/keyword campaign search) is DONE.** The exact-match `system` filter on `/campaigns` was the only filter that existed; browsing now also supports a free-text `q` keyword search (title, description, and system, case-insensitive substring match), combinable with the exact `system` filter (AND, not OR — a deliberate choice: `system` stays a precise filter for players who know exactly what they want, `q` is the fuzzy net for everyone else). See the dated session log entry below for implementation details and judgment calls, including two items flagged for the owner's attention: an unexplained `sz-auth-update.tar.gz` archive found sitting in the mounted `Session-zero` folder (not referenced by this task's instructions or this file — left untouched, not extracted or applied) and the mounted `Session-zero/repo` checkout being on a non-`main` branch (`claude-connectivity-test-1788323368`) with uncommitted changes, so tonight's step-8 sync-to-the-mounted-folder was skipped per the task's own "if dirty, skip and note it" instruction.

**2026-09-06 update (live session): backlog item #8 (player profiles) is DONE.** Signed-in users can now set a bio, a free-text preferred-systems list, and availability at `/profile`, which also shows a "My campaigns" view (campaigns they're DMing and campaigns they're an active/approved player in). Every other user's bio/preferred-systems/availability is visible read-only at `/players/[id]`, linked from DM and roster names on campaign listing/detail pages. See the dated session log entry below for implementation details.

**2026-09-06 update (nightly scheduled build): backlog item #15 (character profiles) is DONE.** Signed-in users can create, edit, and delete lightweight D&D characters (name, class/ancestry/role, a curated emoji "portrait", bio, backstory) at `/profile`, and every character a user has created is visible read-only on their public `/players/[id]` page. This is a foundational item for later backlog items #17 (session logbook) and #19 (character legacies/campaign chronicle). See the dated session log entry below for implementation details and judgment calls.

**2026-09-06 update (live session, continued): backlog #15 was independently built twice in parallel, then merged.** Two sessions worked on this repo at the same time tonight: this one (a live chat session, continuing straight from backlog #8) and the automated nightly-build task, unaware of each other. Both picked backlog #15 (character profiles) as the next unblocked item and both pushed a commit titled "character profiles" within minutes of each other — this live session's push was rejected as non-fast-forward once the nightly build's commit (`7d97d83`) had already landed on `main`. The two designs were genuinely different, not just different code for the same thing: the nightly build's version is account-scoped (a character belongs only to a user, managed on `/profile`, shown on `/players/[id]`, with a curated-emoji portrait picker); this session's original version was campaign-scoped (a character belongs to a specific campaign + player, shown on that campaign's page next to the roster). Rather than force-pushing over the other version or silently picking one, the owner was asked how to resolve it and chose to merge both: kept the nightly build's already-pushed schema and `/profile`-based management as the base, and added an optional `campaign_id` link on top of it, access-checked the same way party notes and session logs are (`hasPrivateAccess` — DM or an approved active member). A character now always belongs to its owner (shown on their profile regardless), and can *additionally* be linked to one campaign at a time (shown on that campaign's page too, with a "Playing in <campaign>" backlink on the profile/public-player views). See the dated session log entry below for the full design writeup.

**2026-09-06 update (live session, continued): backlog item #16 (two-way post-session ratings) is DONE.** A player can rate their DM, and the DM can rate each of their players — a 1-5 star rating plus a handful of toggleable descriptive tags (different tag sets for rating a DM vs. rating a player), on the campaign detail page. Ratings aggregate into a "Reputation" section on `/players/[id]`, split into an "As DM" and "As player" average since those are different signals — a user with no ratings yet shows as "Unrated," not a zero score. Leaving a campaign now also sends both sides a `rating_prompt` notification, the natural moment to ask. See the dated session log entry below for scope decisions (per-campaign rather than per-session, DM<->player only, upsertable so a rating can be revised).

**2026-09-06 update (live session, continued): backlog item #17 (session logbook kudos reactions) is DONE.** Every session log entry on a campaign's detail page now shows a tap-to-react "👏 <count>" button, clickable by the DM or any approved/left party member (the same audience that can already see the private session log), toggling their own kudos on/off. The entry's author gets a `session_log_kudos` notification the first time someone else gives kudos (not on self-kudos, not on un-giving). Visibility is unchanged from the existing private per-campaign log — this item only adds the reaction affordance to entries that already exist; broadening logbook visibility beyond the current party (e.g. to a player's followers) remains an explicit future product decision, not something decided implicitly here. See the dated session log entry below for implementation details.

**2026-09-06 update (live session, continued): backlog item #18 (personal stats dashboard) is DONE.** Both the self-editable `/profile` page and the public `/players/[id]` page now show a "Stats" block: campaigns DMed/played, sessions run/played (derived from session log entry counts, the closest honest proxy this app's data model supports), characters created, distinct systems tried, the most-played system, and the longest-running campaign (by sessions logged, not wall-clock time — this app has no session-duration or campaign-end-date data to compute an elapsed time from). Entirely derived from existing data via a new `lib/stats.ts`; no new tables. Renders unconditionally, even as all-zero/empty/null for a brand-new user, rather than hiding itself. See the dated session log entry below for the exact proxy definitions and judgment calls.

**2026-09-06 update (live session, continued): backlog item #19 (character legacies & campaign chronicle) is DONE.** A character now has a status (still adventuring / retired / fallen) and a free-text epilogue ("how it ended"), settable from `/profile`'s character editor and shown wherever characters already appear (`/profile`, `/players/[id]`, a campaign's page). A campaign's page shows a "Chronicle" line aggregating currently-linked characters by status (e.g. "4 characters passed through · 2 still adventuring · 1 retired · 1 fallen"), and the DM can set a player-visible "danger level" tag (low-lethality / moderate / high-lethality / deadly-OSR) shown right in the campaign header — explicitly framed as a heads-up for prospective players, not a scoreboard. Judgment call: the chronicle counts only *currently* linked characters, not a full historical roster, since this app's character-campaign link is a single current pointer with no retained history of past links — see the dated session log entry below for the full reasoning.

**2026-09-06 update (live session, continued): backlog item #22 (coarse location) is DONE.** Both a user's profile and a campaign now have a free-text, coarse `location` field (e.g. "Austin, TX" or "Online/Remote") — not a geocoded address or precise distance search, which remains explicitly blocked pending a maps API key and a decision on that added complexity/privacy surface. Set from `/profile` and the campaign create/edit forms, shown on `/players/[id]`, the campaign detail page, and campaign browse cards. The `/campaigns` browse page gained a "Location" filter (case-insensitive substring match, same escaping convention as the existing keyword search) alongside the existing system filter and keyword search. See the dated session log entry below for implementation details.

**2026-09-06 update (live session, continued): backlog item #21 (swipe/deck discovery UI) is DONE.** `/campaigns` gained a "Discover" view (toggle next to the existing list view) that presents the same filtered results one at a time as a card, with "Pass" to advance and "View & join" linking to the campaign's own detail page for the actual join flow. Judgment call: this is presentation only — no join/leave action is duplicated in the deck itself, and pass/skip decisions are session-local UI state (not persisted), matching the backlog's own framing of this item as "an alternate presentation of data this app can already list and filter," not new capability. See the dated session log entry below for implementation details.

**2026-09-06 update (live session, continued): backlog item #26 (character portrait upload) is DONE.** "My characters" on `/profile` now supports uploading a real picture (PNG/JPEG/WEBP/GIF, under ~200KB) as a character's portrait, shown in place of the curated emoji avatar everywhere a character appears (`/profile`, `/players/[id]`, a campaign's page). Judgment call: shipped the simplest of the three storage options the backlog line itself documented — the image is stored as a base64 `data:` URL directly in a new nullable `characters.portrait_data_url` column, with no new file/blob storage infrastructure and no dependency on the still-undecided deployment target (item #3). This deliberately bypasses the backlog note that this item "should land after item #3" — a small, size-capped base64 column works identically regardless of where the app eventually deploys, so there was no real reason to keep waiting. No server-side resizing/compression exists; oversized uploads are rejected outright with a clear error rather than silently transformed. See the dated session log entry below for the exact size/validation limits and reasoning.

**2026-09-06 update (live session, continued): backlog item #27, phase 1 (structured weekly availability grid) is DONE.** A signed-in user can now mark a recurring weekly availability grid — 7 days x 4 time-of-day blocks (morning/afternoon/evening/night) — on `/profile`, saved as a full-replace list of day/block cells in a new `availability_slots` table. It's shown read-only on a user's public `/players/[id]` page whenever they've set at least one cell. Judgment call, explicitly scoped: this is phase 1 only, exactly as the backlog line's own text suggested ("data model + picker UI first, then the search/matching layer") — it does *not* build the timezone handling or the actual availability-based search/matching query that would let a DM or player filter by overlap; those remain a separate future phase. The existing free-text `availability` field on profiles is left in place alongside the new structured grid rather than replaced, since removing it now (before the matching layer that would justify replacing it exists) would only lose information users may have already entered. See the dated session log entry below for implementation details.

**2026-09-06 update (live session, continued): backlog item #20, phase 1 (sub request + volunteer pool) is DONE.** A DM or approved member of a campaign can now post "looking for a sub" for an upcoming session from that campaign's own page; any signed-in user can browse every currently-open request app-wide at the new `/subs` page and volunteer (with an optional message); and the requester or DM can see who volunteered and mark the request filled or cancelled. Judgment call, explicitly scoped: this is phase 1 only, exactly as the backlog line's own text suggested ("first the request + volunteer pool, then the three-way approval state machine, then guardrail enforcement") — there is deliberately no three-way approval workflow and no temporary character-custody/guardrail mechanics yet. Picking a volunteer and actually seating them at the table remains a manual, out-of-band decision for this phase; the requester/DM's "mark filled" action is just a status flag, not a handoff of any kind. See the dated session log entry below for implementation details.

**2026-09-06 update (live session, continued): backlog item #9 (campaign page component sprawl) is DONE.** By tonight the campaign detail page had grown to nine feature panels (DmControls, RequestsPanel, RosterPanel, ScheduleForm, PartyNotesPanel, SessionLogPanel, RatingsPanel, SubRequestPanel, plus the page's own characters block) each independently repeating the identical outer-card className string. Extracted a shared `components/Section.tsx` and switched all nine to use it. This is a pure style extraction, not a redesign: each panel keeps its own internal header/content markup exactly as it was, so nothing about how the page looks or behaves changed — verified by the full 174-test suite staying green (including the HTTP integration test that checks rendered HTML across every access role) plus a manual render check counting the shared card class before/after. No product-facing change; a pure internal-quality item from the backlog's own "worth revisiting" notes list.

**2026-09-06 update (live session, continued): backlog item #20, phase 2 (owner/DM approval + temporary custody) is DONE.** A sub request that names a specific character can now go through an approval workflow: the requester or DM picks one volunteer from the pool to move forward as a "placement," the character's owner (always the requester) attaches a free-text guardrails note and approves or declines, the campaign's DM independently approves or declines, and once both approve the character shows a "Currently piloted by \<name\> for a session" badge until the owner or DM explicitly ends it. Per an explicit product decision made before implementation, the rest of the active party is notified of the outcome but isn't a blocking third approval vote — the backlog's original "three-way approval" became two approvers plus an informational notification, to keep the state machine bounded. Guardrails are a free-text note, not the structured default-blocking checkboxes the backlog line originally sketched, and are never programmatically enforced — this app has no gameplay to enforce them during, so "guardrails" here means "agreed-upon instructions for the volunteer," not a rules engine. See the dated session log entry below for the full data model and judgment calls.

**2026-09-02 update: reviewed the original product-design chat and cloud HTML prototype.** The owner shared the Claude.ai chat that produced the original concept (a swipe-to-match, social-network-styled D&D/tabletop app called "Session Zero," system-agnostic, freemium) plus the resulting clickable HTML prototype (`session-zero (4).html`). That prototype is a static mockup with no backend — it never persisted data, authenticated anyone, or talked to a server. It exists to establish the target feature set and visual/product direction; this repo is the real, functioning implementation, built from scratch and currently ahead of the prototype on the campaign-lifecycle/private-side/notification features (none of which the prototype had — it only mocked a swipe deck, a ratings widget, a logbook feed, character legacies, and a sub-request flow, all with fake, hardcoded data and no real accounts). See backlog items 15-24 below for the gap analysis and feasibility read on each prototype feature, now folded into this repo's plan of record.

What the app does today
Session Zero is a Next.js + TypeScript + SQLite app for finding a D&D (or other tabletop) group and tracking a campaign once you're in one.
Real authentication (backlog #2 — done 2026-09-02): a tabbed sign-in/create-account page (`/signin`) backed by `app/api/auth/signup/route.ts` and `app/api/auth/signin/route.ts`. Accounts are stored with a `bcryptjs`-hashed password (`bcrypt.hashSync(password, 10)`); `lib/auth.ts`'s `signUp()` validates a non-blank display name, a password of at least 8 characters, and a not-already-taken (case/whitespace-normalized) email before creating the account, while `signIn()` looks the account up by normalized email and rejects with `AuthError` on an unknown email or a `bcrypt.compareSync` mismatch — the same generic rejection either way, so sign-in doesn't reveal whether an email is registered. Session cookies/tokens are unchanged from the placeholder scheme (`createSession`/`getUserBySessionToken`/`destroySession`); every user-lookup query now explicitly selects columns and excludes `password_hash`, so a hash can never leak into an API response or a rendered page.
Once signed in, a DM can create a campaign (title, description, system, capacity) and players browse/filter (exact `system` match)/sort/paginate the list and request to join. The DM approves or declines requests; the party auto-fills (closes to new requests) once capacity is hit and auto-reopens if someone leaves, or the DM can manually reopen a full campaign to accept more requests without anyone leaving first. The DM can also cancel/un-cancel a campaign (notifying the active party on cancel) and edit its details after posting (capacity can't be dropped below the current approved headcount — validated).
An approved, non-DM member can leave a campaign (with a confirm step in the UI). Leaving marks their membership `left`, re-runs the auto-reopen-if-under-capacity logic, and immediately revokes their access to the campaign's private side.
Once approved, players and the DM get access to the private side of a campaign: a session log the DM can post to, edit, or delete; a next-session date (stored as a UTC instant, with a computed unscheduled/upcoming/past-due status, compared in UTC so it's timezone-safe); and shared party notes (DM + active members can edit). All of this is hidden from strangers, pending requesters, left members, and signed-out visitors — this boundary has explicit regression tests at both the unit layer (`hasPrivateAccess`) and the HTTP-integration layer (driving a real running server with independent per-role cookie jars, checking both API status codes and that the rendered HTML doesn't leak private content to unauthorized viewers).
Everyone gets in-app notifications for the things that affect them, visible in an inbox page (`/notifications`) with an unread badge in the nav bar (polled every 15s — see backlog #6e for the live-push follow-up). The notification types the app defines (`lib/types.ts`): `join_requested`, `join_approved`, `join_declined`, `member_left_dm`, `member_left_party`, `party_notes_updated`, `session_log_posted`, `schedule_updated`, `campaign_cancelled`.
Notification completeness (6a/6b): leaving a campaign notifies the rest of the active party (DM + other approved/active members), not just the DM — `leaveCampaign()` fans a `member_left_party` notification out to every other approved/active member (DM and the leaver excluded; the DM gets their own separate `member_left_dm` notification). Editing the shared party notes notifies the rest of the active party symmetrically (notes are editable by DM + active members — a deliberate judgment call carried over from earlier sessions — so whoever edits notifies everyone else in the active party except themselves).
Per-notification-type preferences (6d): a signed-in user can mute any individual notification type from a settings page (`/settings/notifications`) so that type is never created for them again.
* A `notification_preferences` table keyed by `(user_id, type)` with an `enabled` boolean, defaulting to enabled for every type when no row exists.
* Enforcement lives in `lib/notifications.ts`'s `notify()`/`notifyMany()` — the single choke point every fan-out path goes through. A muted type is checked before the row is ever inserted, so nothing is written for that user (as opposed to writing it and filtering at read time) — the unread badge count and the inbox list are correct for a muted user with zero extra filtering logic anywhere else, at the cost of not being able to later "unmute retroactively" and see history for the period it was muted.
* `app/api/notifications/preferences/route.ts` (GET the full map + human labels, PUT to toggle one type) is the server-side surface; `app/settings/notifications/page.tsx` is the toggle UI.
Live notification push (6e — done 2026-09-02 nightly build): the unread-badge count in `NavBar` no longer relies solely on a 15s poll. `lib/notificationEvents.ts` is a single in-process `EventEmitter`-based pub/sub (one channel per user id); `notify()`, `markRead()`, and `markAllRead()` in `lib/notifications.ts` all publish the user's fresh unread count to it after writing. `app/api/notifications/stream/route.ts` is a `GET` SSE endpoint (auth required, same `requireUser()` as the other notification routes) that sends an immediate unread-count snapshot on connect, then forwards every subsequent publish for that user as an `event: unread` SSE message, plus a comment-line heartbeat every 20s to keep proxies from timing the connection out. `NavBar.tsx` opens an `EventSource` to that endpoint and updates the badge from each `unread` event; if `EventSource` is unavailable (SSR, or a browser/proxy that doesn't support it) or the connection errors out, it falls back to the original 15s-interval polling for that mount (no reconnect/backoff logic on the SSE side — a deliberate scope cut, see session log).
Character profiles (backlog #15 — done 2026-09-06, nightly build): a signed-in user can create, edit, and delete lightweight characters at `/profile` — a name, a free-text class/ancestry/role field (kept system-agnostic, matching this app's existing "preferred systems" approach), a bio, a longer backstory, and a portrait chosen from a curated set of emoji (there's no image-upload infrastructure, so this is a deliberately lightweight placeholder-art system rather than real avatar uploads). Every character a user has created shows up read-only on their public `/players/[id]` page, alongside their profile bio/systems/availability.

Explicitly not yet done (remaining larger backlog item): email notifications (blocked — no SMTP/provider credentials).

Stack
Next.js 16.3.4 (App Router, Turbopack) + TypeScript + Tailwind CSS v4 + better-sqlite3 (file-based, not in-memory) + zod v4 + vitest v4. React 19.2.8.

Backlog (priority order)
1. ~~Persist git history off the ephemeral workspace~~ — DONE 2026-09-02. Repo lives at `https://github.com/Billbiller/Session-zero`, checked out at `C:\Users\billb\Session-zero\repo` on the owner's own laptop, authenticated with the owner's own PAT stored in that machine's local git config (not the cloud sandbox's broken `GH_TOKEN`/`GITHUB_TOKEN` placeholder). Going forward: work in that checkout (via the desktop bridge) and `git push` for real, instead of rebuilding from scratch in a fresh cloud sandbox every session.
2. ~~Real authentication~~ — DONE 2026-09-02. Password-based sign-up/sign-in with `bcryptjs` hashing, validation (name, 8+ char password, unique email), and generic-rejection sign-in error handling. See "What the app does today" above.
3. Deployment decision (SQLite needs persistent-disk hosting, not serverless) — a decision, not a coding task.
4. ~~Campaign tracking UI polish (scheduling/timezone handling)~~ — done.
5. ~~Leave campaign flow~~ — done.
6. Notification polish, split into sub-items:
   * 6a. ~~Leaving a campaign notifies the rest of the party, not just the DM~~ — done.
   * 6b. ~~Party-notes edits notify the rest of the party~~ — done.
   * 6c. Email notifications — not started. No SMTP/email-provider credentials exist yet.
   * 6d. ~~Per-notification-type preferences~~ — done.
   * 6e. ~~Replace unread-badge polling (currently every 15s) with live push (websocket/SSE)~~ — done 2026-09-02 (nightly build). Implemented as Server-Sent Events (not a full websocket) — see "What the app does today" above and the dated session log entry. Polling is kept as an automatic fallback, not removed.
7. ~~Free-text/keyword campaign search~~ — done 2026-09-03 (nightly build). A `q` param on `listCampaigns`/`GET /api/campaigns`/the `/campaigns` UI does a case-insensitive substring match across title, description, and system, ANDed with the existing exact `system` filter rather than replacing it.
8. ~~Player profiles~~ — done 2026-09-06 (live session). `/profile` (self, editable: bio, preferred systems, availability, plus a "My campaigns" DMing/playing view) and `/players/[id]` (anyone, read-only), linked from DM/roster names throughout.
~~9. The campaign detail page (`app/campaigns/[id]/page.tsx`) already composes seven feature components (DmControls, JoinLeaveControls, RequestsPanel, RosterPanel, ScheduleForm, PartyNotesPanel, SessionLogPanel) — worth revisiting for further splitting/a shared layout primitive before more is added to it.~~ **Done 2026-09-06** — extracted a shared `components/Section.tsx` outer-card wrapper, used by all nine feature panels on the page. Pure style extraction, no behavior change.
10. If a general account-settings surface is ever needed (display name changes outside of re-signing-in, etc.), consider whether `/settings/notifications` should become a tab within a broader `/settings` section rather than its own top-level route.
11. `notification_preferences` has no admin/DM-side equivalent — e.g. a DM currently can't see whether their players have muted `join_requested`. Not a bug — muting is a personal choice — but worth a UX pass later (maybe a non-muteable "critical" tier) if this ever surfaces as real user confusion.
12. Tooling notes for future sessions — see below.
13. New tonight: `next/font/google` (Geist/Geist Mono, in the default `create-next-app` template) fails `next build` in network-restricted sandboxes (`Failed to fetch Geist from Google Fonts`) — removed in favor of system font stacks in `app/globals.css`'s `@theme inline` block. If custom webfonts are wanted later, self-host them with `next/font/local` instead of `next/font/google`.
14. New tonight: same-millisecond `ORDER BY created_at DESC` ties (notifications, session log entries, and campaign listings created back-to-back in the same test/request) don't reliably sort newest-first on `created_at` alone since ISO-string timestamps only have millisecond resolution — added `rowid` as a secondary sort key (`ORDER BY created_at DESC, rowid DESC`, and the mirrored `ASC` forms) everywhere "newest/oldest first" matters.
15. ~~Character profiles (bio, art/avatar placeholder, backstory, per-system essentials — class/ancestry for 5e-likes, archetype for others)~~ — done 2026-09-06, merged from two parallel implementations (nightly build + a live session; see the "Current status" note and session log above/below). `characters` table + CRUD at `/api/characters`, editable on `/profile`, read-only on `/players/[id]`. Portrait is a curated emoji set, not an uploaded image (no image-upload infrastructure exists). A character can also optionally be linked to one campaign (access-checked the same way as party notes/session log), surfacing it on that campaign's page next to the roster too. Prerequisite for items 17 and 19 below, which can now build on this.
16. ~~Two-way, post-session ratings~~ — done 2026-09-06 (live session). Players rate the DM, the DM rates players, via a 1-5 star + toggleable descriptive tags (different tag sets per rated-role) on the campaign detail page, upserted per (campaign, rater, ratee) pair so a rating can be revised. Aggregated into an "As DM" / "As player" average + top tags on `/players/[id]` — a user with no ratings shows as **unrated**, not a zero score, matching the source chat's design intent. Judgment call: scoped to per-campaign rather than true per-session (this app has no discrete "session" entity to hang a rating on, only a DM-authored session log and a single next-session date), and strictly DM<->player (no player-to-player ratings, matching the backlog's own wording). Leaving a campaign now sends a `rating_prompt` notification to both sides.
17. ~~Session logbook with reactions ("kudos")~~ — done 2026-09-06 (live session). Every session log entry has a tap-to-react "👏" button; the DM and any approved/left party member can toggle their own kudos on an entry, and the entry's author is notified (`session_log_kudos`) the first time someone else reacts. Judgment call: kept visibility exactly as it already was (the entry's existing private party) — broadening logbook visibility beyond the current party (e.g. to a player's followers) is an explicit future product decision, not folded into this item.
18. ~~Personal stats dashboard~~ — done 2026-09-06 (live session). Campaigns DMed/played, sessions run/played, characters created, distinct systems tried, most-played system, and longest-running campaign (by sessions logged) on both `/profile` (self) and the public `/players/[id]`. Judgment call: dropped "hours at the table" from the shipped scope — this app has no session-duration data anywhere to derive it from honestly, and fabricating an estimate felt worse than omitting it; the other five stats are all directly derivable from existing tables. "Sessions played/run" is a proxy (session log entry counts), not a literal session count, documented as such in the code.
19. ~~Character legacies & campaign chronicle~~ — done 2026-09-06 (live session). Characters gained a status (active/retired/fallen) and a free-text epilogue; campaigns gained a DM-set, player-visible danger level tag and an aggregate "Chronicle" line counting currently-linked characters by status. Judgment call: "characters passed through" in the chronicle means characters *currently* linked to the campaign, not a full historical roster — this app's character-campaign link is a single current pointer (see item 15), with no retained history of past links once a character is unlinked or relinked elsewhere. A fuller "every character who ever passed through" history would need link-history tracking as its own follow-up, not folded into this item.
~~20. Substitute player workflow: a player can post "looking for a sub" for a session; a volunteer pool other users can browse and volunteer into; three-way approval required (character owner sets guardrails, DM approves, active table confirms) before a sub is seated; guardrails default to blocking permanent/irreversible actions (character death, spending rare resources, major story choices) unless the owner explicitly allows them. Rationale: ranked *higher* than the legacy/chronicle features in the source chat's own discussion, because it targets the actual reason campaigns die (attendance/scheduling erosion) rather than engagement polish. Feasible, but the heaviest single item here — a real multi-party approval workflow plus a temporary-custody state on a character, not just a display feature. Should be scoped as its own multi-night effort (e.g., first the request + volunteer pool, then the three-way approval state machine, then guardrail enforcement) rather than attempted in one pass.~~ **Done 2026-09-06.** Phase 1 (the request + browsable volunteer pool, at `/subs` and on each campaign's own page). Phase 2: owner + DM approval (an explicit product decision changed "three-way" to two approvers -- the rest of the active party is notified but isn't a blocking vote, to keep the state machine bounded) plus a temporary-custody marker on the character once both approve. Guardrails exist as a free-text note the owner attaches, not the structured default-blocking checkboxes the backlog line originally described, and there's no programmatic *enforcement* of them (this app has no gameplay to enforce them during) -- see the dated session log entry for the full reasoning.
21. ~~Discovery as a swipe/deck UI~~ — done 2026-09-06 (live session). `/campaigns` gained a "Discover" toggle: the same filtered campaign list, presented one card at a time with "Pass" (advance) and "View & join" (to the real detail page). Judgment call: scoped to campaign cards only, not also player/DM cards — campaigns are the entity this app's browse/filter already covers end-to-end, and a "connect" action on a player/DM card has no obvious real target yet (there's no direct messaging or player-to-player matching in this app to connect *into*), so extending the deck to people would need product scope this backlog line doesn't define. No join/leave logic is duplicated in the deck itself — it only decides what to show; the real membership actions stay on the campaign detail page, the single source of truth. Pass/skip decisions are session-local UI state, not persisted.
22. ~~Location/"near me" discovery~~ — coarse version done 2026-09-06 (live session). Free-text `location` field on profiles and campaigns (e.g. "Austin, TX" or "Online/Remote"), filterable on `/campaigns` via a case-insensitive substring match. Judgment call: shipped only the coarse, free-text level explicitly called out as feasible now — precise geolocation/distance-radius search remains blocked pending a geocoding/maps API key and the privacy-surface decision this backlog line itself flagged; that part is intentionally not attempted here.
23. Freemium tiers (Free vs. "Zero+" paid tier) and any payment processing. Rationale: explicitly part of the original product plan and pricing shown in the prototype ($0 vs $7/mo), but **not feasible to build right now**: it requires a payment processor integration (e.g., Stripe) or, if ever distributed as a native iOS app, Apple's mandatory In-App Purchase — either way that means real business/legal decisions (entity setup, subscription terms, tax handling) and payment-provider credentials this session doesn't have and can't obtain unattended. Blocked on the same kind of human decision as item 6c (email notifications), not a coding gap. Revisit once there's a decision-maker and credentials available; until then, ship features free rather than half-gating them.
24. Native iOS/App Store distribution. Rationale: the source chat's own stated long-term goal, but this repo is a web app (Next.js), and shipping to the App Store either means wrapping it (with real limits on what that can do well) or a separate native build — a large, distinct effort with its own cost/timeline (the source chat's own model estimated ~30 weeks and ~$191K for a full native build, or ~16 weeks/~$26K for a lean, no-code, web-first MVP validation path). Not something to attempt incrementally in nightly sessions; folds into existing backlog item #3 (deployment decision) as a bigger, explicit decision point rather than a task to start coding toward without that decision being made first.
25. Clubs/regional groups and curated community lists (e.g., "best one-shots for new players"). Rationale: mentioned as a lower-tier idea in the source chat's own competitive research, and the same research explicitly warned against adding engagement-bait features that work against getting people to an actual table. Worth doing eventually as light community/discovery features, but correctly the lowest priority of everything above — not scheduled ahead of items 15-22.
26. ~~Character portrait image upload~~ (owner-requested, added 2026-09-06) — done 2026-09-06 (live session), using the simplest of the three storage options this line itself documented: a real uploaded picture (PNG/JPEG/WEBP/GIF, capped at roughly 200KB) stored as a base64 `data:` URL directly in a new `characters.portrait_data_url` column, shown instead of the curated emoji avatar wherever a character appears. Judgment call: deliberately shipped now rather than waiting on item #3 (deployment decision) as this line originally suggested — a small, size-capped base64 column has no dependency on where the app eventually deploys, unlike local-disk or object-storage options, so there was no real blocker left once "simplest option, capped size" was accepted as the tradeoff. No server-side resizing/compression: an oversized upload is rejected with a clear error rather than transformed, keeping scope to validation only. Revisit with real object storage (S3/R2/etc.) once a deployment target is chosen, if image quality/size becomes a real constraint.
~~27. Calendar-based availability + schedule-matching search (owner-requested, added 2026-09-06). Replace the free-text `availability` field on profiles (e.g. "Weeknights after 7pm ET") with structured, queryable availability — a recurring weekly calendar grid (day + time-of-day blocks) a player fills in once — so it becomes a real filter: a DM could search for players free at their table's actual session time, and a player could search campaigns/DMs whose sessions land in their own free blocks, instead of everyone just reading each other's free-text and eyeballing the overlap. Rationale: this is the actual matchmaking payoff the app's name promises, and free-text availability (shipped as part of item #8) was always a placeholder for this. Feasibility: a real structural change, not a quick follow-up — needs (a) a new data model for recurring weekly time-slots per user (not just one text field), (b) a calendar/grid picker UI component (nothing like this exists in the app yet), (c) timezone handling for comparing a player's availability against a campaign's `next_session_at` (the existing UTC-safe schedule-status logic in `lib/schedule.ts` is relevant prior art but only handles a single one-off datetime, not a recurring weekly pattern), and (d) the actual search/filter query once availability is structured. Scope as its own multi-part effort (data model + picker UI first, then the search/matching layer) rather than one pass, similar to item #20.~~ **Phase 1 done 2026-09-06** (data model + weekly day/block picker grid, shown read-only on public profiles). The search/matching layer (comparing two users' grids, timezone handling) explicitly remains for a future phase — see the dated session log entry.
28. Rate the campaign itself, not just the people at the table (owner-requested, added 2026-09-06). Similar to how a rideshare app lets you rate both the driver and the ride. Today's ratings system (item #16, shipped) is strictly person-to-person — a player rates the DM, the DM rates a player, 1-5 stars plus role-specific descriptive tags — with no way to rate the campaign as its own thing (the table's vibe, how well it was run as an experience distinct from the DM personally, whether it delivered on its stated system/genre/tone). Feasibility: a real but bounded extension of existing infrastructure, not a new subsystem — the `ratings` table's `ratee_role` column would need a third value (`'campaign'`) alongside `'dm'`/`'player'`, plus a `CAMPAIGN_RATING_TAGS` array following the same `as const` pattern as `DM_RATING_TAGS`/`PLAYER_RATING_TAGS`. The main design question worth resolving before coding: `ratee_id` currently always points at a `users.id` row (whoever's being rated) — rating a campaign means either relaxing that to also accept a `campaigns.id`, or adding a parallel `ratee_campaign_id` column, either of which is a schema/validation decision worth a deliberate pass rather than bolting on quickly. Who can rate a campaign (any approved/left member, or also the DM rating their own campaign?) and whether it aggregates into a public campaign-level score are both open product questions, not just implementation ones.
29. "Looking for a sub" should carry a specific date, time, and location for the session it's covering (owner-requested, added 2026-09-06). Today's sub request (item #20, shipped) has only a free-text `note` field — a requester might mention "can't make it Sept 20th" in prose, but there's no structured `needed_at`/location data a volunteer could filter or sort by, and no way for the app to show "this sub is needed this Saturday at 7pm" distinctly from the note text. Feasibility: straightforward, following an existing convention — a nullable `needed_at TEXT` column on `sub_requests` (ISO-8601 timestamp, mirroring how `campaigns.next_session_at` already works, including reusing the UTC-safe formatting/display logic in `lib/schedule.ts`) plus a location field (free-text, matching the coarse `location` pattern already used on profiles/campaigns rather than inventing precise geocoding). Worth deciding whether location is even meaningful per-request (most campaigns already have their own `location`/online-vs-in-person setting — a per-request location might mostly matter for in-person games where a sub needs to know exactly where to show up) or whether it should just default from the campaign's own location field with an optional override.
30. Campaigns should carry additional structured info: starting level, genre (within the system), and role-playing style/tone (e.g. horror, heavy combat, roleplay-focused) (owner-requested, added 2026-09-06). Today a campaign only has `system` (free text, e.g. "D&D 5e") plus the recently-added `danger_level` (a closed enum: low-lethality/moderate/high-lethality/deadly-OSR) and `location`. There's no starting level, no genre tag, and no tone/style descriptor — all real filtering/matching signals a prospective player would want before joining (a level 1 horror one-shot reads very differently from a level 10 combat-heavy dungeon crawl in the same system). Feasibility: straightforward, following the `danger_level` convention already established — `starting_level` as a simple text or small-integer field (systems vary enough in how they express level/rank that free text may be more honest than a rigid number, e.g. "Level 3" vs. "Tier 2" vs. a narrative milestone system), and `genre`/`style` as either a curated `as const` array with a label map (matching `DANGER_LEVELS`) if the set of options is worth constraining for filterability, or free text/tags if the space is too open-ended to enumerate honestly. Worth deciding genre/style as a single closed enum vs. a multi-select tag list (a campaign can plausibly be both "heavy battle" and "horror") before implementing, since that shapes both the schema and the campaign browse filter UI.


Tooling notes (read this before re-discovering the same issues)
* This is Next.js 16 — the `create-next-app` scaffold drops a `AGENTS.md`/`CLAUDE.md` pointer at the repo root telling you to read `node_modules/next/dist/docs/` before writing code, because App Router conventions keep changing. Confirmed still true this session: dynamic route `params` (and `searchParams`) are `Promise`s that must be `await`ed (`ctx: { params: Promise<{ id: string }> }`), and `cookies()` from `next/headers` is async (`await cookies()`). "Cache Components" (`cacheComponents: true` in `next.config.ts`) is opt-in in Next 16, not default — this app doesn't enable it, so the pre-16 caching model applies and `export const dynamic = "force-dynamic"` on every route handler that touches cookies/the DB is enough (no `"use cache"` directives needed).
* ESLint flat config for `eslint-config-next@16`: current `create-next-app` scaffolds `eslint.config.mjs` using `defineConfig`/`globalIgnores` from the `eslint/config` package plus `eslint-config-next/core-web-vitals` and `eslint-config-next/typescript` sub-path imports — this shape works out of the box with the `eslint@9` that `create-next-app` itself installs; no manual workaround was needed this session (earlier sessions' notes about `eslint-config-next`'s default export shape and pinning `eslint` below `10.x` may be stale for this scaffold version — re-verify if `npx eslint .` breaks on a fresh install).
* `react-hooks/set-state-in-effect`: a `useEffect` whose body calls a `load()` function declared at component scope (needed when a button handler must also trigger the same reload) gets flagged even though `load()` only sets state after an internal `await`. Fix is a scoped `// eslint-disable-next-line react-hooks/set-state-in-effect` directly above the `load()` call — **the disable comment must be the line immediately before the flagged call**; multi-line explanatory comments have to go *above* the disable directive, not between it and the code, or the directive silently doesn't attach (ESLint then reports both "unused disable directive" and the original error). `NavBar`'s polling effect avoids the whole issue by declaring its loader function inside the effect body itself.
* `next build` type-checking test files: `tsconfig.json`'s default `include` picks up every `**/*.ts`/`**/*.tsx`, which pulls Vitest test files into the Next.js build's TypeScript pass and fails it. Fix: add `"__tests__/**"` and `"vitest.config.ts"` to tsconfig's `"exclude"` array.
* `next.config.ts` cannot carry an `eslint` key in this Next.js version — ESLint config now lives solely in `eslint.config.mjs`.
* `@types/react-dom` doesn't publish a version matching `react-dom`'s own version number — let npm resolve `@types/react`/`@types/react-dom` against their own `^19` ranges rather than pinning to the runtime package's exact version.
* `next/font/google` requires outbound access to `fonts.googleapis.com` at build time — fails hard in a network-restricted sandbox. Use system font stacks (or `next/font/local` with self-hosted font files) instead.
* HTTP integration test spawning a real server: spawn the **local** `next` binary directly (`node_modules/.bin/next start`), not via `npx next start` — `npx` forks a wrapper process that doesn't reliably forward a kill signal on to the actual `next-server` child, which left orphaned servers holding the spare port open between test runs (`EADDRINUSE` on the next run). Spawn with `detached: true` and kill the whole process group with `process.kill(-server.pid, "SIGTERM")` in `afterAll`.
* Same-millisecond `ORDER BY created_at DESC` ties need a secondary sort key — see backlog item #14 above.
* Per-test-file isolated SQLite databases: `lib/db.ts` reads `process.env.SQLITE_DB_PATH` at module-load time (falling back to `data/session-zero.db` for the real app). `__tests__/setup.ts` is wired in via `vitest.config.ts`'s `setupFiles` and assigns a fresh `data/test-<uuid>.db` path before that test file's own imports resolve — Vitest isolates each test file's module registry, so this gives every test *file* (not every individual test) its own DB. The HTTP integration test manages its own separate DB file explicitly (passed via `env` to the spawned `next build`/`next start` child processes).
* `package.json` needs `"type": "module"` for `vitest.config.ts`'s `export default` to load natively without a CJS-compat warning; harmless with `next build`/`next lint`/the test suite since every source file already uses ESM syntax.
* `vitest.config.ts` should use `path.dirname(fileURLToPath(import.meta.url))` instead of bare `__dirname`, which Vite's native config loader warns is unsupported and will eventually be removed.

Testing
196 tests total: 190 unit tests across `__tests__/unit/*.test.ts` for the business logic in `lib/` (real auth — signup validation, duplicate-email rejection, sign-in success/failure, session token lifecycle; campaign CRUD + capacity-floor validation + browse/filter/sort/paginate, membership join/approve/decline/leave + auto-fill/auto-reopen/manual reopen, the private-access boundary function + active-party-membership helper, notification create/fan-out/read tracking plus the notificationEvents pub/sub publishing an unread-count update on notify()/markRead()/markAllRead() and only to the matching user's channel, per-notification-type preference get/set/default behavior plus enforcement at notification-creation time, session log CRUD, party notes + its notify-on-edit fan-out including a muted-preference case, UTC-safe schedule-status computation including a UTC-midnight boundary case, free-text keyword campaign search across title/description/system (case-insensitivity, ANDing with the exact `system` filter, and `%`/`_` being treated as literal characters rather than SQL `LIKE` wildcards), and player profiles (default-empty profile for a new user, save/re-read with whitespace trimming, partial updates, length-limit rejection, per-user isolation, splitting the stored preferred-systems string for display, and the "my campaigns" DMing/playing lists including exclusion of pending/declined/left memberships)), and characters (deterministic default-avatar assignment from a curated set, trimming, blank-name rejection, per-field length-limit rejection, an unrecognized avatarEmoji value falling back to a valid default, per-user listing newest-first and isolated from other users' characters, partial updates, ownership enforcement rejecting another user's edit/delete attempts, and — merged in from the campaign-scoped design — creating a character pre-linked to a campaign the owner is DMing or an approved member of, rejecting a link to a campaign the owner has no access to or that doesn't exist, defaulting to unlinked, linking/relinking/unlinking via update, rejecting a relink to an inaccessible campaign while leaving the existing link intact, an omitted campaignId on update leaving the existing link untouched, and campaign-scoped listing returning only that campaign's characters oldest-first), and ratings (a player rating the DM and the DM rating a player, rejecting player-to-player/self/stranger ratings, rejecting an out-of-range star value and a tag that doesn't belong to the ratee's role, upserting a second rating instead of duplicating, still allowing a rating after the player has left, listing the right rating targets for a DM vs. a player, an empty target list for someone with no relationship to the campaign, a brand-new user reporting as unrated rather than zero, averaging multiple ratings with per-role tag tallies, and the rating_prompt notification firing to both sides on leave), and session log kudos (an active party member toggling kudos on/off an entry, kudos count and the viewer's own given/not-given state being correct per viewer, the session_log_kudos notification firing to the entry's author on a fresh kudos from someone else but not on self-kudos or on un-giving, and a stranger with no relationship to the campaign being rejected), and stats (all-zero/empty/null for a brand-new user, campaigns-DMed plus sessionsRun counted correctly, campaigns-played plus sessionsPlayed counted correctly and still counting entries posted after the player left, characters-created counted, distinct systems and the most-played system derived correctly across both DM and player roles, and the campaign with the most session log entries picked as the longest campaign), and character legacies/campaign chronicle (a new character defaulting to active status with an empty epilogue, creating directly as retired/fallen with an epilogue, updating status and epilogue independently of other fields, rejecting an over-length epilogue, an unrecognized status value falling back to the current/default status, chronicle counts aggregating correctly by status among currently-linked characters and excluding unlinked ones, a character dropping out of the chronicle once unlinked, a new campaign defaulting to a null danger level, the DM setting/changing/clearing the danger level, and an omitted danger level on update leaving it untouched), and coarse location (a profile's default empty location, saving/re-reading it, rejecting an over-length value, a campaign defaulting to an empty location and having it set on create/update, and browse-page filtering by a case-insensitive substring match on location including a no-match case), and character portrait upload (a new character defaulting to a null portrait, creating with a valid portrait data URL, rejecting a non-image or malformed data URL, rejecting an over-length data URL, the owner setting/replacing/removing a portrait via update, and an existing portrait staying untouched when omitted from an update), and weekly availability (an empty grid for a brand-new user, saving and reading back slots in day-then-block order, a second save fully replacing rather than merging the grid, clearing the grid with an empty array, de-duplicating repeated day/block pairs, rejecting an out-of-range day and an unrecognized block, and per-user grid isolation), and sub requests (the DM or an approved member posting a request defaulting to open, rejecting a post from someone with no relationship to the campaign, rejecting an over-length note, open requests appearing in the app-wide pool and disappearing once filled/cancelled while still showing on the campaign's own list, listings enriched with campaign/requester display context and volunteer counts, a stranger volunteering and viewerHasVolunteered reflecting correctly per-viewer, rejecting the requester volunteering for their own request, rejecting volunteering on a non-open request, a second volunteer call updating the existing message instead of duplicating, withdrawing a volunteer, restricting the volunteer list to the requester or DM, notifying the requester and DM once each on a fresh volunteer but not on a message update, the requester or DM marking a request filled/cancelled only from open, and rejecting a status change from someone who isn't the requester or DM), and sub placements (the requester or DM selecting a volunteer to create a pending placement, rejecting a selection from a non-requester/DM, rejecting a selection of someone who hasn't actually volunteered, rejecting a placement for a characterless request, rejecting a second pending placement while one exists, confirming and setting temporary custody once both owner and DM approve regardless of approval order, an owner or DM decline ending the placement immediately without waiting on the other, rejecting a review from someone who isn't the actual owner/DM, rejecting a review of an already-resolved placement, cancelling a pending placement and re-opening the request for a new one, rejecting a cancel from a non-requester/DM, ending a sub clearing the character's temporary-pilot marker while leaving the historical placement record untouched, rejecting an end-sub from someone who isn't the owner/DM, the sub_placement_resolved notification firing to the volunteer/owner/DM/rest of the active party on resolution, and listing every placement for a request including resolved ones); plus 6 HTTP integration tests in `__tests__/integration/http.test.ts` that build and start a real production server (`next build` + `next start` on a spare port) and drive it with independent per-role cookie jars: the full join/approve/auto-close/leave/auto-reopen flow, the full privacy boundary (API status codes and rendered-HTML content checks across DM/approved-member/pending-requester/left-member/stranger/signed-out-visitor), the 6a/6b notification-fanout scenarios end-to-end, the 6d mute-a-type-via-the-settings-endpoint scenario end-to-end, a real-password end-to-end auth test (signup, duplicate-signup rejection, wrong-password rejection, correct-password sign-in unlocking an authenticated endpoint, unknown-email rejection), and a 6e test that opens a real SSE connection to `/api/notifications/stream` (rejecting a signed-out request first), reads the immediate `unreadCount: 0` snapshot off the wire, triggers a join-approval so the player is notified, and reads the follow-up `unread` event proving the push arrived without polling. (Backlog #21's Discover deck added no new lib-level logic — it's a presentational client component with local `useState` cycling over data the existing `listCampaigns`/`approvedHeadcount` calls already provide and already have coverage above — so it has no new unit tests, matching this repo's existing convention of relying on lib-test + manual-smoke-test coverage for presentational-only React components like `CharacterManager`/`RatingsPanel`/`SessionLogPanel` rather than component-level rendering tests, since no React component testing setup — jsdom, Testing Library — exists in this project.)

Session log
2026-08-29 through 2026-09-01: see prior entries in this file's git history for the full narrative (initial build-out through the 6a/6b/6d notification work). Summary: placeholder sign-in, campaign CRUD, browse/filter/sort/paginate, join-request + approve/decline flow with auto-fill/auto-reopen/manual-reopen, leave-campaign flow, UTC-safe scheduling, the private-side boundary (session log, party notes) with unit + HTTP-integration regression coverage, in-app notifications with 6a/6b fan-out completions, and 6d per-notification-type preferences — all established across several from-scratch rebuilds in the ephemeral cloud sandbox, each one starting by reading this file since nothing else survived between sessions.

2026-09-02 (tonight)
Rebuilt the app from scratch in a fresh cloud sandbox to parity with the 2026-09-01 spec (68 tests: 64 unit + 4 integration; build/lint/test all clean; manual smoke test of `/`, `/campaigns`, `/signin`, and a 404 case against a spare-port production server). Then solved backlog item #1 for real, at the owner's request, using this session's link to their desktop app:
* Confirmed the desktop bridge reaches "bill-25-laptop" and requested access to `C:\Users\billb\Session-zero` (a plain, non-OneDrive path — the OneDrive-redirected `Documents` folder couldn't be granted, likely because it's a symlink/junction the bridge won't follow).
* Found the bridge's shell (`device_bash`) runs inside an isolated Ubuntu 22.04 VM on the laptop, not the user's actual Windows environment — so no pre-existing git/GitHub credentials were available there either. It does have real outbound internet access (unlike the cloud sandbox) and no `sudo`.
* Installed the GitHub CLI from the official release tarball into `$HOME/bin` (no root needed).
* Had the owner generate a GitHub personal access token and drop it into a temporary `github_token.txt` file inside the connected folder (rather than pasting it into the chat transcript), read it once, configured `git` (`user.name`/`user.email`, `credential.helper store`, a `~/.git-credentials` entry using the token), verified `git ls-remote` against `https://github.com/Billbiller/Session-zero.git` succeeded, then deleted the token file (had to request one-time delete permission on the folder first — `device_bash` can't delete inside mounted folders by default).
* Cloned the repo (`C:\Users\billb\Session-zero\repo`) — found just the auto-generated README, confirming no app code had ever reached GitHub before.
* Copied tonight's freshly-built app from the cloud workspace into that checkout, wrote this progress file, and made the first real commit + push.
Net effect: backlog item #1 is resolved. Future sessions should work directly in the laptop checkout (via the desktop bridge) rather than rebuilding from scratch in the cloud sandbox each time — the whole reason this file existed as a rebuild-from-spec crutch no longer applies once history genuinely persists. This file is kept going forward as ordinary project documentation, not as a memory workaround.

Later the same night: attempted to set up a recurring nightly scheduled task (cloud Routine with `requires_local_device: true`, "every night at 10pm PST, do the next backlog item or, if the app is fully functional and tested, propose and build the next customer-facing feature") so this work would happen automatically instead of in ad hoc chat sessions. This surfaced a real gap in the tooling: creating the trigger only ever reports `no_signed_approval — this task will run in the cloud only`; the actual device-binding approval is a separate step in the desktop app's own Scheduled Tasks UI, invisible to (and uncontrollable from) the session that created the trigger. After one accidental delete-via-settings and a recreate, the owner completed that approval in the app UI and confirmed via screenshots it was bound correctly (Active, "Only on this computer", "Always allowed — Automatically approve", next run ~10:00 PM). A manual "Run now" test still failed with "the device bridge to the laptop isn't connected" even though this chat session's own bridge was live at that moment — so the bound task's own device-connection step is not yet reliable, for reasons not yet root-caused (possibly a timing/liveness check that's stricter than what this session's tools see). Per the owner's instruction, the fallback for now is: keep doing the "nightly" increment manually, in a live chat session like this one, rather than relying on the scheduled task, until its reliability is confirmed. Revisit the automated schedule later rather than continuing to debug it blind tonight.
Then did tonight's actual increment by hand: implemented backlog item #2, real authentication, end to end (see "What the app does today" and the backlog above) — added `bcryptjs`/`@types/bcryptjs`, rewrote `lib/auth.ts` and the two `/api/auth/*` routes, rewrote the `/signin` page with a sign-in/create-account UI, migrated `users` to add `password_hash`, and updated every test that used to call the old placeholder `findOrCreateUser` to go through real signup instead (68 → 75 tests, all green; build and lint both clean). Transferred the changes from the cloud workspace to the laptop checkout and pushed to `main` (see commit referenced in this same push).

2026-09-02 (nightly scheduled build)
This was the first run of the new automated nightly scheduled task (the "device bridge" approach mentioned above was replaced today with a mounted-folder + GitHub-token setup — see the SETUP note in the task definition). Ran fully unattended per that task's instructions: no clarifying questions, judgment calls documented here instead.

Picked backlog item #6e (replace unread-badge polling with live push) as the night's work: it's the next unblocked item in priority order — item #3 (deployment decision) is explicitly a human decision, not a coding task, and item #6c (email notifications) is explicitly blocked on SMTP credentials that don't exist. Chose Server-Sent Events over a websocket: this app already speaks plain HTTP/fetch everywhere, SSE needs no new dependency or protocol upgrade handling, and the actual requirement (server -> client one-way "your unread count changed" push) doesn't need websockets' bidirectionality. Documented as SSE, not "websocket/SSE", in the backlog going forward so this reads as a deliberate choice rather than an open question.

Implementation: `lib/notificationEvents.ts` (new) is a single in-process `EventEmitter` pub/sub keyed by user id. `lib/notifications.ts`'s `notify()`, `markRead()`, and `markAllRead()` each publish the caller's fresh unread count after writing. `app/api/notifications/stream/route.ts` (new) is an authenticated `GET` SSE endpoint (`export const runtime = "nodejs"` — it touches cookies and better-sqlite3, both of which need Node, not edge) that sends an immediate snapshot on connect, streams `event: unread` messages as they're published, and sends a heartbeat comment every 20s. `components/NavBar.tsx` now opens an `EventSource` to it and falls back to the original 15s polling if `EventSource` is unavailable or the connection errors — no reconnect/backoff logic was written for the SSE side itself (`EventSource`'s built-in auto-reconnect covers transient drops; a hard, repeated failure is treated as "fall back to polling for this page load" rather than retried indefinitely, a scope cut to keep this a one-night change). Added a `notificationEvents.test.ts` unit suite (publish-on-notify/markRead/markAllRead, per-user channel isolation, muted-type suppression) and one new HTTP integration test that opens a real SSE connection, reads the initial snapshot off the wire, triggers a join-approval, and asserts the push arrives without polling. 75 -> 80 tests, all green; build and lint both clean.

Judgment call / process note worth flagging: this session's tools mount the shared folder at two different paths — a Windows-style path for the Read/Write/Edit/Grep/Glob tools (which is the *live* `C:\Users\billb\Session-zero\repo` checkout, i.e. the one the owner browses on their laptop) and a separate `/sessions/.../mnt/Session-zero` path for the bash sandbox, where `~/build/session-zero` (the actual git scratch clone used for all git operations, per the task's own setup instructions) is a completely different directory. Early in this session the notification-stream code was written with Write/Edit directly against the Windows-style path — i.e. directly into the *live* mounted checkout — before noticing it, rather than into the scratch clone as instructed. Caught it when `next build` in the scratch clone didn't pick up the new route. Recovered by: copying the correct file contents from the mounted checkout into the scratch clone (so the scratch clone, where build/lint/test/commit actually happen, has the real changes); reverting the three pre-existing files that had been edited in place in the mounted checkout (`lib/notifications.ts`, `components/NavBar.tsx`, `__tests__/integration/http.test.ts`) back to their last-committed content via `git show HEAD:<path>` from the scratch clone; but two brand-new files created directly in the mounted checkout (`lib/notificationEvents.ts`, `app/api/notifications/stream/route.ts`) could not be deleted from there — file deletion in that mounted folder returned "Operation not permitted", and the interactive delete-permission tool isn't usable in this unattended run. Net effect: the mounted checkout now has 2 leftover untracked files (content-identical to what's in the real commit) sitting alongside its otherwise-unchanged tracked files. Step 8 (sync the mounted checkout after pushing) will see this as a dirty working tree and its own logic will skip the copy-over rather than risk clobbering something — see this session's final summary for what that means for the owner's local folder, and consider deleting those two stray files by hand (or just `git status`/`git clean` there, if that folder's own git can be trusted again) so future runs start from a clean mounted checkout.

2026-09-02 (backlog review, ad hoc session)
Owner shared two source materials for broader context: the original Claude.ai design chat that produced the "Session Zero" concept (swipe-to-match social-network framing, system-agnostic, freemium, plus a string of follow-on features added over that conversation: ratings, session logbook, character legacies/campaign chronicle, substitute players, market/competitive analysis, and cost/business modeling), and the resulting static HTML prototype (`session-zero (4).html`) with no backend. Read both in full and reconciled them against this repo's actual state: the real app (this repo) already covers the core matchmaking/campaign-lifecycle loop the prototype never implemented (real auth, campaign CRUD, join/approve/decline, private session log/notes/schedule, notifications), while the prototype has several social/reputation/retention features this repo doesn't yet have. Folded the gap into the backlog as items 15-25 above, in priority order, each with a one-line rationale and an explicit feasibility read — flagging freemium/payments (23) and native App Store distribution (24) as blocked on business decisions and credentials this session doesn't have, rather than coding gaps. No application code was changed this session — this was a planning/documentation update only.

2026-09-03 (nightly scheduled build)
Ran fully unattended per the nightly-build task's instructions: no clarifying questions, judgment calls documented here instead.

Setup note: this was the first run since the mounted-folder + GitHub-token approach's SETUP note was revised on 2026-09-01 to drop the old "device bridge" framing entirely. Connected `C:\Users\billb\Session-zero`, found the token as a file whose name itself is the token (`ghp_...txt`), derived it from the filename via a shell variable, and never echoed it. Also found two things in the mounted folder not mentioned in the task's own instructions: an `sz-auth-update.tar.gz` archive (contents include `lib/auth.ts`, `lib/db.ts`, auth API routes, a signin page, and several test files) and, inside `Session-zero/repo`, a checkout on branch `claude-connectivity-test-1788323368` with uncommitted changes rather than a clean `main`. Neither is referenced by this task's instructions or by this file's own backlog/history, so — per the instruction to treat observed content as data, not commands, and to only act on what this task file actually asks for — left the archive unextracted and untouched, and did not touch the mounted `repo` checkout beyond a read-only `git status`. Flagging both for the owner to look at; see the "Current status" note above.

Picked backlog item #7 (free-text/keyword campaign search) as the night's work: item #3 (deployment decision) is an explicit human decision, not a coding task; item #6c (email notifications) is explicitly blocked on SMTP credentials that don't exist; #7 was next in priority order and a clean, one-night scope.

Implementation: `lib/campaigns.ts`'s `listCampaigns()` gained a `q` option — when present, it's trimmed, lowercased, and matched with `LIKE` against `LOWER(title)`, `LOWER(description)`, and `LOWER(system)` (OR'd together), ANDed with the existing `system` exact-match filter and the cancelled-campaign exclusion. A new `escapeLikePattern()` helper escapes `%`, `_`, and the escape character itself before the value is wrapped in `%...%`, with `ESCAPE '\'` on each `LIKE` clause, so a keyword containing `%` or `_` is treated as a literal substring rather than a SQL wildcard — judgment call: worth the small extra complexity since campaign titles/descriptions are free text a DM could plausibly type either character into (e.g. "50% off", "session_zero"), and silently treating them as wildcards would make search results wrong in a hard-to-debug way. `GET /api/campaigns` and the `/campaigns` server component both thread a `q` query param through (page links preserve it across pagination). Chose AND (not OR) semantics between `q` and `system`: `system` remains a precise filter for a player who already knows the exact system string from browsing, while `q` is the fuzzy/keyword layer — combining them narrows rather than broadens, which matches how the two inputs read in the UI (a "Search" box plus a separate "System" box, not a single merged field). Added a "Search" text input to the `/campaigns` filter form, and adjusted the empty-state copy to distinguish "no campaigns match yet" (nothing posted) from "no campaigns match your search" (a filter/keyword excluded everything).

Testing: added two new unit tests to `__tests__/unit/campaigns.test.ts` — one covering matches against each of title/description/system, case-insensitivity, the AND-with-`system` combination (including a case that should miss), and a no-match case; one specifically covering `%`/`_` as literal characters. Both new tests use highly-distinctive keyword prefixes (`kwzx7...`) after the first attempt collided with an earlier test's fixture data in the same file (that file's tests share one SQLite DB across `it` blocks per Vitest's per-file module isolation, not per-test — a keyword search is far more collision-prone against leftover fixture data than the exact-match tests elsewhere in this file, which already used `Unique ...` prefixes for the same reason). 80 -> 82 tests, all green; build and lint both clean.

No email/SMTP work attempted (backlog #6c remains blocked, as documented). No changes made outside `lib/campaigns.ts`, `app/api/campaigns/route.ts`, `app/campaigns/page.tsx`, and `__tests__/unit/campaigns.test.ts`.

2026-09-06 (live session, backlog #8: player profiles)
Owner asked to run the next phase of development in a live chat session (not the nightly unattended task) right after fixing last night's push-permission blocker. Picked backlog item #8, next in priority order after #7 (item #3 is a human deployment decision; #6c is blocked on SMTP credentials that still don't exist).

Data model: a new `profiles` table (`user_id` PK/FK to `users`, `bio`, `preferred_systems`, `availability` all TEXT NOT NULL DEFAULT '', `updated_at`). Judgment call: stored `preferred_systems` as one free-text comma-separated string rather than a normalized child table — this app already has real per-system data via campaigns' own `system` field for matching/filtering purposes, so a player's "preferred systems" is closer to a personal-bio field than a structured taxonomy; a simple string keeps the UI to one text input and avoids over-building before there's a real need to query "find players who prefer X." `lib/profiles.ts` mirrors the `notification_preferences` pattern already in this codebase: `getProfile()` returns an implicit empty default when no row exists yet (no row is written until a user actually saves), `upsertProfile()` does a partial update (only overwrites fields actually passed in) with trimming and a length cap on each field (2000 chars bio, 300 chars each for preferred-systems and availability), and `splitPreferredSystems()` turns the stored string into a clean array for chip-style display. Also added `myCampaigns(userId)`, returning `{ dming, playing }` — `dming` is every campaign where the user is `dm_id`; `playing` is campaigns where the user has an `approved`-status membership (pending/declined/left are correctly excluded, including a membership that *was* approved and then left). Both queries use the existing `ORDER BY ... , rowid` tie-break convention from this file's tooling notes (#14) for same-millisecond timestamps.

Routes: `GET`/`PUT /api/profile` (auth required; GET also returns `dming`/`playing` so the profile page is a single round trip) and a public, unauthenticated `app/players/[id]/page.tsx` showing another user's bio/preferred-systems/availability read-only (404 via `notFound()` for an unknown id). `app/profile/page.tsx` is a client component following this repo's existing pattern (see `NotificationSettingsPage`/`NotificationsPage`) — fetch-on-mount, redirect to `/signin` on a 401 rather than rendering an empty/broken form. Linked from the NavBar (clicking your own display name now goes to `/profile`, replacing the old plain-text name) and from every DM/roster display name on the campaign browse list, campaign detail header, and `RosterPanel` (all now link to `/players/[id]`).

Judgment call on scope: kept the public `/players/[id]` view to bio/preferred-systems/availability only — did not also expose a stranger's own "my campaigns" DMing/playing list there. A campaign's DM is already public (shown right on the campaign itself), so linking to a DM's profile doesn't leak anything new, but showing a full list of every campaign a random player has joined felt like it exposes more about a stranger's activity than this feature needs to for a first pass; that's a separate, deliberate product decision to revisit later rather than default into it. Availability was included on the public view (not just the self-editable one) since surfacing it to other players is the actual point of a matchmaking app — the field exists for the person's own campaign-mates and interested DMs to see, not just for their own reference.

Testing: added `__tests__/unit/profiles.test.ts` (9 tests) covering the default-empty-profile case, save/re-read with trimming, partial updates, length-limit rejection on all three fields, per-user isolation, `splitPreferredSystems()`, and `myCampaigns()` including the pending/declined/left-exclusion case. Also ran an ad hoc manual smoke test against a real `next start` server on a spare port (not part of the committed test suite, just a live sanity check before committing): unauthenticated `GET /api/profile` → 401; signup → default empty profile; `PUT` → save; re-`GET` → values persisted; an oversized bio → 400; the public `/players/[id]` page rendered the saved bio/systems/availability and 404'd for an unknown id; and the campaign browse page's DM name rendered as a working `/players/<id>` link after posting a campaign. 82 → 91 tests, all green; build and lint both clean.

No changes to email/SMTP (backlog #6c, still blocked) or to the deployment decision (#3, still a human call).

2026-09-06 (nightly scheduled build)
Ran fully unattended per the nightly-build task's instructions: no clarifying questions, judgment calls documented here instead.

Setup note: found two candidate token files in the mounted `Session-zero` folder — `New token.txt` (a `github_pat_...` fine-grained token, modified 2026-09-03) and an older `ghp_mB3faw7agag6nzNJY5wnvCMjOdyzMX0.txt` (filename-as-token, modified 2026-09-01, predating this task's own 2026-09-01 rewrite). Used the more recent `New token.txt`, verified with a `git ls-remote` before doing anything else; it worked. Also present but untouched (not referenced by this task's instructions): `sz-auth-update.tar.gz` and the `repo` checkout, exactly as flagged by a prior session — left both alone again.

Picked backlog item #15 (character profiles) as the night's work. Item #3 (deployment decision) is an explicit human call, not a coding task; item #6c (email notifications) is blocked on SMTP credentials that don't exist; items #9-11 are open-ended "worth revisiting" notes rather than scoped, buildable features; #15 was the next item in the list with a clear spec and an explicit "feasible now" read, and unblocks #17 and #19 later.

Implementation: a new `characters` table (`id`, `user_id`, `name`, `archetype`, `bio`, `backstory`, `avatar_emoji`, timestamps) and `lib/characters.ts` (`createCharacter`/`updateCharacter`/`deleteCharacter`/`getCharacter`/`listCharactersForUser`, a `CharacterError` class, following the exact shape of `lib/profiles.ts`: trimming, per-field length caps — 100/150/1000/4000 chars for name/archetype/bio/backstory — and ownership checks on update/delete). `GET`/`POST /api/characters` and `PATCH`/`DELETE /api/characters/[id]` mirror the `profiles`/`sessionLog` route conventions (zod body validation, `requireUser()`, ownership errors mapped to 403). `/profile` gained a new `CharacterManager` client component (add/edit/delete, with a confirm step before delete, matching the existing leave-campaign confirm pattern) above the "My campaigns" section; the public `/players/[id]` page gained a read-only characters list. A new `CharacterSummary` presentational component is shared between the editable and read-only views so they can't drift apart.

Two judgment calls worth flagging:
* No image-upload infrastructure exists in this app, so "art/avatar placeholder" (as scoped in the backlog line) is a curated set of 20 fantasy-themed emoji rather than an uploaded picture — a real avatar-upload system is a much bigger, separate feature (storage, moderation, resizing) that wasn't in scope for a one-night item. A character's portrait defaults deterministically (a hash of the name into the emoji set) so it's never blank, and can be overridden from a dropdown.
* Kept "class/ancestry/role" as one free-text `archetype` field rather than per-system structured fields (a 5e class dropdown vs. a different system's archetype taxonomy, etc.) — this app is deliberately system-agnostic throughout (see `preferred_systems` and campaigns' own free-text `system` field), so a single free-text field matches that existing pattern rather than introducing a new per-system special case.
* Caught and fixed a real bug before it shipped: initially imported the new `CHARACTER_AVATARS` constant into the client component `CharacterManager.tsx` from `lib/characters.ts`, which imports `lib/db.ts` (better-sqlite3, a native Node module) — that would have pulled a DB driver into the browser bundle. `next build`'s successful compile doesn't catch this at the type level since it's still valid TypeScript; caught it by checking the existing convention (`lib/types.ts` already holds `NOTIFICATION_TYPES`/`NOTIFICATION_LABELS` as the client-safe home for shared constants, and `app/settings/notifications/page.tsx` never imports server `lib/` modules directly). Fixed by moving `CHARACTER_AVATARS` to `lib/types.ts` and having `lib/characters.ts` import and re-export it for server-side callers.

Testing: added `__tests__/unit/characters.test.ts` (10 tests) covering deterministic default-avatar assignment, an explicit avatar choice, blank-name rejection, all four per-field length limits, an invalid `avatarEmoji` value falling back to a valid default, per-user listing (newest-first, isolated from other users), partial updates, ownership enforcement on edit/delete, actual deletion, and not-found errors. 91 → 101 tests, all green; build and lint both clean. Also ran an ad hoc manual smoke test against a real `next start` server on a spare port: unauthenticated `GET /api/characters` → 401; signup → empty list; create a character → 201 with the expected fields; an oversized bio → 400; update → persisted; the public `/players/[id]` page rendered the character's name and updated bio; delete → 200 and the list was empty again.

No changes to email/SMTP (#6c, still blocked) or the deployment decision (#3, still a human call).

2026-09-06 (live session, continued: merging two parallel backlog #15 implementations)
This session had already built and committed its own campaign-scoped character design locally (name, class/archetype, ancestry/lineage, backstory, a text/emoji avatar, scoped to one campaign per character) when `git push` was rejected as non-fast-forward: the automated nightly-build task had, unaware of this session, independently built and pushed its own account-scoped character design (`7d97d83`) in the same window. Fetched and read the other commit in full (`git show 7d97d83`) before doing anything else, rather than guessing at the conflict or force-pushing over it. The two designs were incompatible at the schema level (two different `characters` tables) and reflected genuinely different product interpretations of the same backlog line, not a simple merge-conflict-in-code situation — so this was surfaced to the owner as a real decision (keep one, keep the other, or combine both) rather than resolved unilaterally. The owner chose to merge both.

Design of the merge: reset this session's scratch clone to `origin/main` (the nightly build's now-canonical `7d97d83`), keeping its schema, `lib/characters.ts` conventions (account-scoped `createCharacter(userId, input)`/`updateCharacter(characterId, userId, input)`/`deleteCharacter`, the curated `CHARACTER_AVATARS` emoji set with deterministic-hash defaulting, and the `/profile`-based `CharacterManager` UI) as the base, since it was already the one pushed to `main`. Added a single nullable `campaign_id` column on top of it rather than reintroducing a second table or a second CRUD surface — a character still always belongs to its owner first (created/edited/deleted only via the account-level `/api/characters` routes, always visible on that owner's `/players/[id]`), and can *additionally* be linked to one campaign at a time. Linking (on create via `campaignId` in the POST body, or on update via `campaignId` in the PATCH body — `undefined` leaves the link alone, `null` unlinks, a campaign id (re)links) reuses `hasPrivateAccess(userId, campaignId)` — the exact same DM-or-approved-member check as party notes and session logs — so a character can't be attached to a campaign its owner isn't actually part of; unlinking never needs that check, since removing a claim can't spoof anything the way adding one could. `listCharactersForCampaign(campaignId)` is new, public (no access check, matching the roster's own visibility), and feeds a new read-only "Characters at this table" block on the campaign detail page (next to `RosterPanel`) via the existing `CharacterSummary` presentational component — so all three surfaces (`/profile`'s editable list, `/players/[id]`'s read-only list, and now the campaign page's read-only list) render from the same component and can't drift apart. `CharacterSummary` gained an optional `linkedCampaign` prop (shown everywhere except the campaign's own page, where it would be redundant) that links back to the campaign. `GET /api/characters` now enriches each character with its linked campaign's current title server-side, so the profile page can show/link to it without a second round trip and stays correct even for a campaign the owner has since left. `CharacterManager`'s create/edit form gained a "Playing in" `<select>` populated from the signed-in user's own current (non-cancelled) campaigns — passed down from `/profile`'s existing `dming`/`playing` state — with a fallback option injected if a character is linked to a campaign that's since fallen out of that list, so editing a character never silently drops an existing link just because the dropdown re-rendered.

Testing: kept all 10 of the nightly build's original `characters.test.ts` tests verbatim (still valid against the merged `lib/characters.ts`, which is backward-compatible with every call site that doesn't pass `campaignId`) and appended 9 new tests covering: creating a character pre-linked to a campaign the owner is DMing or an approved member of; rejecting a link to a campaign the owner has no access to, and to a campaign that doesn't exist; defaulting to unlinked; linking, relinking to a different campaign, and unlinking via update; rejecting a relink to an inaccessible campaign while confirming the existing link is left untouched; confirming an omitted `campaignId` on update leaves an existing link alone; and campaign-scoped listing returning only that campaign's characters, oldest first. 101 → 110 tests, all green; build and lint both clean. Also re-ran a full manual smoke test against a real `next start` server covering the merged flow specifically: created an unlinked character via `/api/characters`, confirmed a non-owner's attempt to link it 403s, confirmed the owner linking their own character to their own campaign succeeds, confirmed a stranger creating a brand-new character pre-linked to a campaign they don't belong to is rejected, confirmed the public campaign-scoped endpoint and the campaign detail page both show the linked character, and confirmed the public player page shows the character with a working "Playing in <campaign>" link back.

Judgment call: chose not to try to reconcile the two commits' git history (e.g. rebasing, cherry-picking pieces of the discarded local commit) — the two implementations shared no code (different table shape, different files, different component names), so a clean rewrite on top of the already-pushed commit was more honest about what actually happened than a contrived merge commit would have been. The discarded local commit (`51a0e45`, campaign-scoped only, never pushed) was dropped entirely once its concept (optional campaign linking) was folded into the merged design.

No changes to email/SMTP (#6c, still blocked) or the deployment decision (#3, still a human call).

2026-09-06 (live session, continued: backlog #16, two-way post-session ratings)
Owner asked to keep going through the backlog for the rest of the day, one item at a time with a full test/commit/push cycle each. Picked #16 (ratings) as the next item: it's explicitly called out in this file as an owner-stated must-have and the trust layer the whole matching pitch depends on, and #3/#6c remain the only hard blockers (human decision / missing credentials) ahead of it in priority order.

Data model: a new `ratings` table (`id`, `campaign_id`, `rater_id`, `ratee_id`, `ratee_role` — `'dm'` or `'player'`, `stars` 1-5, `tags` a JSON-array string, timestamps), with a `UNIQUE (campaign_id, rater_id, ratee_id)` constraint so a second rating from the same rater upserts rather than duplicates — letting a rating be revised as a campaign goes on rather than locked in after one submission. `lib/ratings.ts`'s `rateCampaignParticipant()` derives each side's role in the campaign (`dm`, `player` via an approved-or-left membership, or `null`) and requires the rater and ratee to be on *opposite* roles — this both enforces the backlog's explicit DM<->player scope (no player-rates-player, no self-rating) and rejects strangers/pending-requesters/decliners in one check, without a separate permission function. `ratee_role` is stored directly on the row (denormalized from which side the ratee was playing) rather than derived via a join every read, since it's fixed at creation and this app already denormalizes similarly elsewhere.

Judgment call — scope narrowed from the backlog's literal "post-session" framing to per-campaign: this app has no discrete "session" entity (session log entries are free-text DM notes, not structured records with their own IDs a rating could attach to; `next_session_at` is a single upcoming date, not a history of past ones). Modeling a true per-session rating would need a new sessions table as a prerequisite, which felt like scope creep for this item — a per-campaign rating (upsertable, so it can reflect how things are going as the campaign progresses) is a reasonable, honest interpretation of "how has this DM/player been" and matches the "running average over many sessions" framing from the backlog's own carried-over design note better than a single one-off review would anyway.

Two curated tag sets (`DM_RATING_TAGS`, `PLAYER_RATING_TAGS` — 8 each, in `lib/types.ts` alongside `NOTIFICATION_TYPES`/`CHARACTER_AVATARS` as the established home for client-safe shared constants) since "great narrator" and "fair rulings" don't make sense for a player, and "team player"/"good roleplayer" don't make sense for a DM; a rating is validated against whichever set matches the *ratee's* role. `getUserRatingSummary(userId)` aggregates by `ratee_role` into `asDm`/`asPlayer` separately (a good DM and a good player are different signals, same reasoning as separating the two in the tag sets) — `average: null` when `count === 0` so a brand-new member renders as "Unrated," never as a 0-star score.

Routes: `GET`/`POST /api/campaigns/[id]/ratings` — GET returns the signed-in viewer's role, their eligible rating targets (the DM's approved/left players, or just the DM for a player) each with any existing rating, and the correct tag-option list for whichever role they're about to rate; POST creates/updates one rating. `components/RatingsPanel.tsx` (client, on the campaign detail page) renders nothing at all if the viewer has no relationship to the campaign — the panel's own emptiness is the "you're not eligible" signal, no separate error state needed in the UI. `/players/[id]` gained a "Reputation" block (As DM / As player, star display, rating count, top 3 tags) right under the display name, ahead of the bio — reputation is exactly the kind of at-a-glance trust signal a matchmaking app's whole pitch depends on, so it earns the prominent slot.

Notification: added a new `rating_prompt` type (flows through the existing `NOTIFICATION_TYPES`/`notify()`/preferences infrastructure automatically — no new plumbing needed there) fired from `leaveCampaign()` to both the leaving player ("rate the DM") and the DM ("rate them") — leaving is the one clear, already-instrumented "this relationship is wrapping up" moment in the app, versus e.g. prompting after every session log post (too noisy) or on a schedule (no session-completion event exists to hang it on).

Testing: `__tests__/unit/ratings.test.ts` (13 tests) covering both rating directions, rejecting player-to-player/self/stranger ratings, out-of-range stars, a tag from the wrong role's set, upsert-not-duplicate on a second rating, rating still being allowed after the ratee has left, the correct target list for a DM vs. a player, an empty list for someone with no relationship to the campaign, a fresh user reporting as unrated, multi-rating averaging with per-role tag tallies, and the `rating_prompt` notification firing to both sides on leave. 110 → 123 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server confirmed the full flow end-to-end: eligibility (approve → GET targets → rate), the invalid-tag-for-role rejection, the stranger rejection, the public reputation display rendering correctly, both `rating_prompt` notifications firing on leave, and the DM still being able to rate a player who's since left.

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #17, session logbook kudos reactions)
Owner asked to keep iterating through the backlog for the rest of the day, one item per full implement/test/ship cycle. Picked #17 next: it's the next unblocked item after #16 in priority order (#3 and #6c remain the only hard blockers — human deployment decision and missing SMTP credentials respectively), and it builds directly on the session log that already exists.

Implementation: a new `session_log_kudos` table (`id`, `entry_id` FK, `user_id` FK, `created_at`) with a `UNIQUE (entry_id, user_id)` constraint — one kudos per user per entry, toggled rather than stackable, matching the backlog's own "lightweight tap-to-react" framing (a like button, not a counter a single person can inflate). `lib/sessionLog.ts` gained `listEntriesWithKudos(campaignId, viewerId)` (the existing `listEntries()` plus each entry's kudos count and whether the given viewer has already reacted) and `toggleKudos(entryId, userId)` (inserts if absent, deletes if present, gated by the same `hasPrivateAccess()` check the session log itself already uses — DM or an approved/left party member, not just the DM who can post). A fresh kudos (not an un-kudos, and never for reacting to your own entry) fires a new `session_log_kudos` notification to the entry's author, flowing through the existing `NOTIFICATION_TYPES`/`notify()`/preferences infrastructure with no additional plumbing. New route `POST /api/campaigns/[id]/log/[entryId]/kudos` calls `toggleKudos()`, mapping `SessionLogError` to 403 the same way every other session log route does. The existing `GET /api/campaigns/[id]/log` now calls `listEntriesWithKudos()` instead of `listEntries()`, and the campaign detail page passes the signed-in viewer's id down to `SessionLogPanel`, which renders a "👏 <count>" pill per entry — filled/highlighted when the viewer has already reacted, clickable by any signed-in party member (not gated to DM-only the way edit/delete are).

Judgment call: did not attempt to decide or build anything about broadening logbook visibility beyond the existing party (e.g. to a player's followers) — the backlog line explicitly flagged that as a separate product decision to make first, and this item's scope is the reaction affordance on entries that are already private to the party, not a visibility change.

Testing: extended `__tests__/unit/sessionLog.test.ts` with 4 new tests (up from 6 to 10) covering: an active party member giving then un-giving kudos on an entry (count and `viewerGaveKudos` correct at each step); a second viewer who hasn't reacted seeing `viewerGaveKudos: false` without affecting the count; the `session_log_kudos` notification firing to the author on a fresh kudos from someone else but not on self-kudos and not on un-giving; and a stranger with no relationship to the campaign being rejected. 123 → 127 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server (fresh SQLite db on a spare port) drove the full flow end-to-end via independent per-role cookie jars: signup DM + player, create campaign, join/approve, DM posts a session log entry, player gives kudos (count 1, `viewerGaveKudos` true for the player and false for the DM), player un-gives kudos (count back to 0), a signed-up stranger attempting kudos got a 403, and the DM giving kudos on their own entry did not add a duplicate notification beyond the one already fired by the player's earlier kudos.

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #18, personal stats dashboard)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. Picked #18 next: the next unblocked item after #17 in priority order (#3 and #6c remain the only hard blockers), and its own backlog line already called out that it's derivable from existing data with no new data model.

Implementation: new `lib/stats.ts` with a single `getUserStats(userId)` returning `campaignsAsDm`, `campaignsAsPlayer`, `sessionsRun`, `sessionsPlayed`, `charactersCreated`, `systemsPlayed` (distinct), `mostPlayedSystem`, and `longestCampaign` (`{ id, title, sessionCount }` or null) — all computed via aggregation queries against `campaigns`, `memberships`, `session_log_entries`, and `characters`, no new tables. `UserStats` lives in `lib/types.ts` (the established client-safe home for shared shapes, per the `NOTIFICATION_TYPES`/`CHARACTER_AVATARS`/rating-tag precedent) since it needs to be importable by both a server component (`/players/[id]`) and a client component (`/profile`); `lib/stats.ts` re-exports the type for server-side callers. New presentational `components/StatsPanel.tsx` (no "use client" needed — pure display, no state or fetching of its own) renders identically from both call sites. `/players/[id]` calls `getUserStats()` directly (it's already a server component). `/profile` (a client component) gets stats from an extended `GET /api/profile` response (`stats: getUserStats(auth.user.id)` alongside the existing `profile`/`dming`/`playing`), one round trip, matching how that route already bundles multiple related pieces of data.

Judgment call — scope narrowed from the backlog line's literal wording: dropped "hours at the table" from what shipped. This app tracks no session start/end time or duration anywhere (session log entries are free-text recaps with just a `created_at`, not structured records with a duration field), so there's no honest way to derive actual hours played — estimating one (e.g. "assume every session is 3 hours") would be fabricating a number that reads as real data, which felt like the wrong tradeoff versus just not shipping that one stat yet. The other five ("sessions run"/"sessions played", "systems tried", "most played system", "longest campaign", "characters created") are all directly and honestly derivable from data this app already has. "Sessions run"/"sessions played" are explicitly documented in the code as a *proxy*, not a literal count: each session log entry represents one recap a DM posted after a session, so counting entries is the closest honest measure available — a DM who ran a session but forgot to post a recap won't show up, and this is called out in the doc comments rather than presented as ground truth. "Longest campaign" is measured by sessions logged, not wall-clock duration, for the same missing-duration-data reason.

Testing: new `__tests__/unit/stats.test.ts` (6 tests) covering: all-zero/empty/null output for a brand-new user; campaigns-DMed and sessionsRun counted correctly; campaigns-played and sessionsPlayed counted correctly, including entries posted *after* the player left (a left member's stats still reflect the sessions they were actually part of, matching the ratings feature's own "still allows rating after leaving" precedent); characters-created counted; distinct systems and the most-played system derived correctly across both a DM's and a player's combined DM+player campaigns; and the campaign with the most session log entries correctly picked as the longest campaign. 127 → 133 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server (fresh SQLite db, spare port): `GET /api/profile` for a brand-new user returned all-zero/empty/null stats; created a campaign, joined/approved a player, posted two session log entries, and re-fetched `/api/profile` for both the DM (`campaignsAsDm: 1`, `sessionsRun: 2`, `mostPlayedSystem: "5e"`, `longestCampaign` populated) and the player (`campaignsAsPlayer: 1`, `sessionsPlayed: 2`) — both correct; fetched the DM's public `/players/[id]` page HTML directly and confirmed the Stats section actually renders (grepped for "Sessions run", "Campaigns DMed", "Most played system", "Longest-running campaign", all present).

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #19, character legacies & campaign chronicle)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. Picked #19 next: it's explicitly noted in its own backlog line as "the source chat's own strongest idea" and as feasible now that item 15 (character profiles) exists as a prerequisite — the next unblocked item after #18.

Data model: `characters` gained `status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active','retired','fallen'))` and `epilogue TEXT NOT NULL DEFAULT ''`; `campaigns` gained `danger_level TEXT CHECK (danger_level IS NULL OR danger_level IN ('low-lethality','moderate','high-lethality','deadly-osr'))`, nullable so "not set" is the honest default rather than forcing every DM to pick one. Added the same lightweight-migration pattern already established for `users.password_hash` (a `PRAGMA table_info` check plus `ALTER TABLE ... ADD COLUMN` for databases created before this change) — SQLite can't add a CHECK constraint via `ALTER TABLE ADD COLUMN`, so migrated databases rely on `lib/characters.ts`/`lib/campaigns.ts` validation rather than the schema itself for those two columns; new databases get the full constraint from the `CREATE TABLE` statements.

`CHARACTER_STATUSES`/`CHARACTER_STATUS_LABELS` and `DANGER_LEVELS`/`DANGER_LEVEL_LABELS` live in `lib/types.ts` alongside the established client-safe constants (`NOTIFICATION_TYPES`, `CHARACTER_AVATARS`, the rating tag sets). `lib/characters.ts`'s `createCharacter`/`updateCharacter` accept optional `status`/`epilogue` (defaulting a new character to `"active"` with an empty epilogue; an unrecognized status value is ignored and falls back to the current/default rather than erroring, matching the existing `avatarEmoji` fallback pattern). New `getCampaignChronicle(campaignId)` aggregates currently-linked characters by status into `{ active, retired, fallen, total }`. `lib/campaigns.ts`'s `updateCampaign` gained a `dangerLevel` update (undefined = unchanged, null = clear, a recognized level = set), validated against `DANGER_LEVELS`.

UI: `CharacterManager`'s create/edit form gained a status `<select>` and, only shown once status isn't "active," an epilogue textarea. `CharacterSummary` (shared by all three character-display surfaces) shows a status badge next to the name when not active (styled distinctly for "fallen" — a red-tinted border — versus "retired") and the epilogue in italics beneath the backstory. `DmControls`'s edit form gained a danger-level `<select>` with an explicit "not a scoreboard" caption. The campaign detail page shows the danger level as a small badge in the header (visible to anyone, not gated behind private access — the whole point is a heads-up for a prospective player deciding whether to join) and a one-line "Chronicle: N characters passed through · X still adventuring · Y retired · Z fallen" summary above the existing "Characters at this table" list (only the non-zero segments are shown).

Judgment call: scoped "characters passed through" in the chronicle to characters *currently* linked to the campaign, not a true historical roster of every character that was ever part of it. This app's character-campaign link (from item 15) is a single current pointer (`campaign_id`, nullable, one at a time) with no retained history of past links — once a character is unlinked or relinked elsewhere, this app has no record it was ever there. Building a full "every character who ever passed through, even after leaving" history would need a separate link-history table as its own follow-up; reading the current chronicle as "right now," not "ever," is an honest reflection of what this app's existing data model actually tracks, matching the same kind of proxy-not-literal framing already used for backlog #18's "sessions run/played."

Testing: extended `__tests__/unit/characters.test.ts` with 7 new tests (19 → 26) covering: a new character defaulting to active status with an empty epilogue; creating directly as retired/fallen with an epilogue; updating status and epilogue independently of other fields; rejecting an over-length epilogue; an unrecognized status value falling back to the default; chronicle counts aggregating correctly by status among currently-linked characters (and excluding an unlinked one); and a character dropping out of the chronicle once unlinked. Extended `__tests__/unit/campaigns.test.ts` with 3 new tests (10 → 13) covering: a new campaign defaulting to a null danger level; the DM setting, changing, and clearing it; and an omitted `dangerLevel` on update leaving an existing value untouched. 133 → 143 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server (fresh SQLite db, spare port) drove the full flow end-to-end: DM set a danger level (visible in the campaign header HTML), a non-DM's attempt to change it correctly errored, a player joined/was approved/created a character linked to the campaign (active by default), the campaign page's Chronicle line correctly read "1 character passed through · 1 still adventuring," the player then marked their character as fallen with an epilogue, the Chronicle line updated to reflect the fallen character, the public `/players/[id]` page rendered both the "Fallen" badge and the epilogue text, and both an invalid danger level and an invalid character status were correctly rejected (400) by their respective API routes.

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #22, coarse location)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. Picked #22 next: items #20 (substitute player workflow) and #23/#24 (freemium/payments, native iOS) are explicitly flagged as either a much heavier multi-night effort or blocked on business/human decisions this session can't make unattended; #21 (swipe/deck discovery UI) is a lower-priority UX layer over data this app can already list; #22's own backlog line explicitly calls out the coarse (non-geocoded) version as "feasible now," making it the next reasonably-scoped item.

Data model: `profiles` and `campaigns` each gained a `location TEXT NOT NULL DEFAULT ''` column — free text, deliberately not a geocoded address or lat/long pair, matching the backlog's own explicit scope cut (precise distance search needs a maps API key this session doesn't have, plus a privacy-surface decision about storing exact locations, both flagged as blocking a *precise* version, not the coarse one shipped here). Added the same lightweight-migration pattern used for every prior schema change this session (PRAGMA check + `ALTER TABLE ADD COLUMN` for pre-existing databases).

`lib/profiles.ts`'s `upsertProfile()` accepts an optional `location`, trimmed and capped at 200 characters like the other profile text fields. `lib/campaigns.ts`'s `createCampaign()` accepts an optional `location` (defaulting to `""`), `updateCampaign()` accepts a `location` update, and `listCampaigns()` gained a `location` filter option — a case-insensitive substring match (`LOWER(location) LIKE ... ESCAPE '\'`) reusing the exact same `escapeLikePattern()` helper and `%`/`_`-as-literal-character convention already established for the free-text keyword search (backlog #7), so filtering by "Austin" matches a campaign whose location is "Austin, TX" without also treating a literal `%` or `_` in someone's location string as a wildcard.

UI: a "Location" text input added to `/profile`'s self-edit form, the "Post a campaign" form, and `DmControls`' campaign-edit form, all following the exact same pattern as this app's other free-text fields (availability, preferred systems, description). Displayed on `/players/[id]` (a new "Location" section, shown only if set), the campaign detail page header (`&middot; <location>` alongside system/DM/headcount/danger-level), and each campaign card on the `/campaigns` browse list. The browse page's filter form gained a "Location" input alongside the existing Search/System/Sort controls, threaded through pagination links the same way `system`/`q` already are.

Testing: extended `__tests__/unit/profiles.test.ts` with 2 new assertions/tests (default empty location, saving/re-reading it, and rejecting an over-length value) and `__tests__/unit/campaigns.test.ts` with 2 new tests (a campaign defaulting to an empty location and having it set via create/update, and browse-page-style filtering by a case-insensitive location substring including a no-match case). 143 → 146 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server: a fresh profile's location defaulted to `""`, saving and re-reading `"Austin, TX"` round-tripped correctly and rendered on the public `/players/[id]` page, two campaigns created with different locations were correctly filtered by `?location=austin` and `?location=online` via the API, both locations rendered correctly on the browse page HTML, and an over-200-character location on campaign creation was correctly rejected with a 400.

No changes to email/SMTP (#6c) or the deployment decision (#3). Precise geolocation/distance search remains explicitly out of scope, per the backlog line's own stated blocker (no maps API key, and a privacy decision not yet made).

2026-09-06 (live session, continued: backlog #21, swipe/deck discovery UI)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. Picked #21 next: items #20 (substitute player workflow) and #23/#24 (freemium/payments, native iOS) remain explicitly flagged as either a much heavier multi-night effort or blocked on business/human decisions this session can't make unattended; #21, while ranked lower priority than items 15-20 by the backlog's own ordering, is explicitly called out as feasible now (a UX layer over data this app can already list and filter), making it the next reasonably one-night-scoped item once #22 was done.

Implementation: new `components/DiscoverDeck.tsx` (client component) takes the same filtered campaign list the existing browse view already computes and presents it one card at a time — title, system, DM name, headcount/capacity, location, danger level, and description — with a "Pass" button (advances a local `useState` index, no persistence) and a "View & join" link straight to that campaign's own detail page. `/campaigns` gained a List/Discover toggle (a `view` query param, so it's linkable/shareable like the existing filters) right next to "Post a campaign." In discover mode the page fetches a larger batch (50) instead of the list view's page size of 10, and skips rendering numbered pagination — a "swipe through these" flow doesn't fit paging, one continuous deck does.

Two judgment calls worth flagging. First: no join/leave action lives inside the deck itself — clicking "View & join" always lands on the real campaign detail page, which is already the single source of truth for a viewer's membership state (pending/approved/etc.) via `JoinLeaveControls`. Duplicating that logic into the deck card would mean two parallel places membership state could drift, for a feature explicitly scoped as "an alternate presentation," not new capability — so the deck only decides *what to show*, never *what to do about it*. Second: scoped to campaign cards only, not also player/DM cards as swipeable entities (the backlog line mentions "player, DM, and campaign cards") — this app has no direct messaging or player-to-player matching for a "connect" action on a person-card to plug into yet, so building swipeable people-cards now would mean inventing a whole connect/match workflow this backlog item doesn't define, well beyond "make browsing more visual." Campaigns are the one entity this app's browse/filter/join loop already fully supports end to end, so that's what the deck wraps.

Testing: no new unit tests — this item added no `lib/` logic at all, only a presentational client component driven by data (`listCampaigns`, `approvedHeadcount`, `getUserById`) that already has full unit-test coverage from prior backlog items. This matches the existing convention in this codebase of not writing component-level rendering tests for presentational UI (`CharacterManager`, `RatingsPanel`, `SessionLogPanel`, etc. have none either) — there's no jsdom/Testing Library setup in this project, only `lib/` unit tests and real-server HTTP integration tests. 146 tests still green; build and lint both clean. Manual smoke test against a real `next start` server: created two campaigns sharing a unique test system, confirmed the default list view still renders both, confirmed `?view=discover` returns 200 and the server-rendered payload contains the first card's full data (title, description, system, location, headcount, DM name) plus the "1 of 2" progress indicator and both the "Pass"/"View & join" controls and the List/Discover toggle links.

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #26, character portrait image upload)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. Revisited item #26 (owner-requested, added earlier this session) rather than moving on to a genuinely blocked item: its own backlog text had already documented three storage options and flagged only the storage decision as the blocker, one of which (base64-in-SQLite) needs no new infrastructure and no credentials at all — making this a reasonable "feasible now" candidate on closer look, not something that actually has to wait for item #3.

Data model: `characters` gained a nullable `portrait_data_url TEXT` column (the standard lightweight-migration pattern for existing databases, same as every prior schema change tonight). `null` means no uploaded portrait, in which case `avatar_emoji` is shown as before — fully backward compatible, no existing character's display changes.

Validation, in `lib/characters.ts`'s new `validatePortraitDataUrl()`: rejects anything not matching `^data:image\/(png|jpeg|jpg|webp|gif);base64,` (wrong MIME type or not a data URL at all), and rejects anything over 280,000 characters — base64 inflates size by roughly a third, so that string-length cap corresponds to source images up to roughly 200KB, small enough to not meaningfully bloat the SQLite file while still allowing a real compressed photo or drawing. `createCharacter`/`updateCharacter` both thread an optional `portraitDataUrl` through this validator (undefined = unchanged on update, null = explicitly remove, a valid data URL = set/replace). The zod schemas on both character API routes mirror the same length cap as an early reject before the request body is even fully parsed, with the real validation staying in `lib/characters.ts` as the source of truth.

UI: `CharacterManager`'s form gained a file `<input accept="image/png,image/jpeg,image/webp,image/gif">` next to the existing emoji picker, with a live preview (the uploaded image if present, the emoji otherwise) and a "Remove" button once a portrait is set. Client-side, `FileReader.readAsDataURL()` converts the selected file to a data URL, after a size check (200KB raw, mirroring the server's ~200KB-source budget) that rejects an oversized file immediately rather than after a wasted upload+encode round trip. `CharacterSummary` (shared by every character-display surface — `/profile`, `/players/[id]`, and a campaign's page) renders the portrait as a small rounded `<img>` when set, falling back to the emoji span otherwise, so all three views stay in sync automatically.

Judgment call, explicitly flagged: this bypasses the backlog line's own note that the item "should land after item #3 (deployment decision)." That guidance made sense for the local-disk and object-storage options (both genuinely need to know where the app runs, or need their own credentials), but doesn't apply to base64-in-SQLite — a small, size-capped text column works identically no matter where this app eventually deploys. Revisiting the note on its merits rather than treating it as an unconditional gate felt like the honest reading here, especially since the owner explicitly asked for this feature. Also explicitly not built: server-side resizing/compression. An oversized or wrong-format upload is rejected with a clear message, not transformed — keeping this pass to validation only, matching the backlog's own "basic upload hygiene" framing rather than a full image-processing pipeline.

Testing: extended `__tests__/unit/characters.test.ts` with 6 new tests (26 → 32): a new character defaulting to a null portrait; creating directly with a valid portrait data URL; rejecting a non-image-MIME or malformed (non-data-URL) string; rejecting an over-length data URL; the owner setting, replacing, and removing a portrait via three successive updates; and an existing portrait staying untouched when omitted from an unrelated update. 146 → 152 tests, all green; build and lint both clean (including fixing a real ESLint gotcha caught during this item: a two-line `// eslint-disable-next-line` comment only suppresses the literal next line — the second comment line consumed that slot, leaving the actual `<img>` element on the third line unprotected and still flagged; fixed by collapsing the disable comment plus its justification onto a single line immediately before the element, in both `CharacterManager.tsx` and `CharacterSummary.tsx`). Manual smoke test against a real `next start` server: a fresh character defaulted to a null portrait; setting a small valid `data:image/png;base64,...` value via `PATCH` persisted correctly; the public `/players/[id]` page's rendered HTML contained the actual `data:image/png;base64,...` string as an `<img src>` attribute (confirmed by direct grep of the response body, not just the API response); an oversized (300,000+ character) data URL was rejected with 400; a non-image MIME type and a garbage non-data-URL string were both rejected with 403 (`CharacterError` mapped to 403, consistent with every other character-update rejection); and removing the portrait (`portraitDataUrl: null`) correctly cleared it back to null.

No changes to email/SMTP (#6c) or the deployment decision (#3) — this item's whole point was proving it didn't actually need to wait on #3.

2026-09-06 (live session, continued: backlog #27 phase 1, structured weekly availability grid)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. All six of the reasonably one-night-scoped, currently-unblocked backlog items had been shipped (#17, #18, #19, #22, #21, #26); the remaining items are each either a genuine human/business decision this session can't make (#3 deployment, #23 payments, #24 App Store distribution, all blocked on credentials or owner decisions), blocked on missing credentials (#6c email), open-ended process notes rather than scoped features (#9-#11), or explicitly flagged in the backlog's own text as needing to be scoped as their own multi-night effort rather than attempted in one pass (#20 substitute-player workflow, #27 calendar availability). Rather than force a full multi-night item into one pass, or stop while a defensible slice remained, took #27's own suggested scoping literally: it names "(a) a new data model for recurring weekly time-slots per user... (b) a calendar/grid picker UI component... then (c) timezone handling... and (d) the actual search/filter" as the shape of the work, and explicitly says to "scope as its own multi-part effort (data model + picker UI first, then the search/matching layer)." This session did exactly that first slice — (a) and (b) only — and stopped there.

Data model: new `availability_slots` table (`user_id`, `day_of_week` 0-6, `block` one of morning/afternoon/evening/night, composite primary key on all three) — a plain new table, not a migration, since it's additive with no prior column to backfill. `day_of_week` is explicitly documented as an app-level display-order index (0=Monday..6=Sunday) unrelated to `Date.prototype.getDay()` (which is 0=Sunday) — there's deliberately no calendar date backing a *recurring* weekly pattern, so tying it to JS's day numbering would just invite confusion later. No timezone is stored; the grid represents the user's own subjective week, same scope-limiting choice as the existing free-text `availability` field.

Backend: `lib/availability.ts` exports `getAvailabilitySlots(userId)` and `setAvailabilitySlots(userId, slots)` — the latter is a full-replace (delete-then-reinsert in one transaction), not a per-cell toggle endpoint, because the UI always has and submits the complete current grid state rather than tracking individual diffs. Validates each slot's day (integer 0-6) and block (one of the four known values) before writing anything, and silently de-duplicates repeated day/block pairs rather than erroring on them (a user's own double-click shouldn't be a 400). `AVAILABILITY_DAYS`/`AVAILABILITY_BLOCKS`/`AvailabilityBlock`/`AvailabilitySlot` all live in `lib/types.ts` (not `lib/availability.ts`), following this session's established client-safe-constants convention so the new `AvailabilityGrid` client component can import them without pulling in `lib/db.ts`.

API: folded into the existing `GET`/`PUT /api/profile` route rather than a new endpoint — `GET` now also returns `availabilitySlots`, and `PUT`'s body schema gained an optional `availabilitySlots` array (max 28 entries, one per possible cell) that's a full-replace when present and leaves the stored grid untouched when omitted, matching every other field's partial-update semantics on this same route.

UI: new `components/AvailabilityGrid.tsx`, a controlled 7×4 table of toggle buttons (`aria-pressed`, `aria-label`s naming the day and block for screen readers) that takes `slots`/`onChange`/`editable` props and owns no fetch/save logic itself, matching `CharacterManager`'s controlled-form pattern. Used in two places: editable on `/profile` (folded into the existing single profile form and its one Save button, submitted alongside bio/systems/availability/location in the same `PUT`), and read-only on `/players/[id]` (only rendered when the user has at least one slot set, so a user who hasn't touched this feature yet shows nothing new).

Judgment call, explicitly flagged: kept the existing free-text `availability` field on profiles as-is, alongside the new structured grid, rather than replacing it as the backlog line's longer-term framing implies ("replace the free-text `availability` field... with structured, queryable availability"). Replacing it now would risk silently discarding whatever users have already written there, for no present benefit — the matching/search layer that would actually need the structured data instead of the free text doesn't exist yet in this phase. Revisit the free-text field's fate once phase 2 (search/matching) is actually built. Also explicitly not built in this phase: timezone handling (item (c)) and the search/filter query itself (item (d)) — both remain for a future phase, as the backlog's own scoping note anticipated.

Testing: new `__tests__/unit/availability.test.ts` (8 tests) covering: an empty grid for a brand-new user; saving a set of slots and reading them back in day-then-block order; a second save fully replacing rather than merging the grid; clearing the grid with an empty array; de-duplicating repeated day/block pairs instead of erroring; rejecting an out-of-range day; rejecting an unrecognized block; and per-user grid isolation. 152 → 160 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server (fresh SQLite db, spare port): a fresh profile's `GET` returned an empty `availabilitySlots` array; `PUT` with two slots persisted and round-tripped correctly on the next `GET`; `PUT` with an invalid block ("midnight") and an out-of-range day (9) were both correctly rejected with 400 and a clear zod error message; a `PUT` touching only an unrelated field (`bio`) left the previously-saved grid untouched; the public `/players/[id]` page's rendered HTML contained exactly 28 `aria-pressed` grid cells (7 days × 4 blocks) reflecting the saved slots; and a brand-new user's `/players/[id]` page correctly rendered no "Weekly availability" section at all (grid-empty case hides the block entirely, matching the existing convention for other optional profile sections like location/availability text).

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #20 phase 1, sub request + volunteer pool)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. With #27's phase 1 shipped, the only other item with a backlog-endorsed narrow first slice left was #20 (substitute player workflow) — its own text names the same three-phase shape used for #27: "first the request + volunteer pool, then the three-way approval state machine, then guardrail enforcement." This session shipped exactly that first phase and stopped there, the same discipline applied to #27 earlier tonight.

Data model: two new tables, `sub_requests` (campaign_id, requester_id, note, status open/filled/cancelled) and `sub_volunteers` (request_id, volunteer_id, message, unique per request+volunteer so volunteering twice updates rather than duplicates) — both purely additive, no migration needed.

Backend (`lib/subRequests.ts`): `createSubRequest` is gated behind `hasPrivateAccess` (the same DM-or-approved-member boundary as session log/party notes) since only someone actually at the table has a session to fill. `listSubRequestsForCampaign` and `listOpenSubRequests` are deliberately public/ungated — like the roster and character list already on a campaign's page, inviting an outside volunteer is the whole point, so browsing isn't restricted to existing members. Both return a `SubRequestSummary` (campaign title/system, requester name, volunteer count, whether the current viewer has already volunteered) computed via a shared `withContext` join helper, with `SubRequestSummary`/`SubVolunteerWithName` living in `lib/types.ts` rather than `lib/subRequests.ts` — same client-safe-types convention established earlier this session for `UserStats`/`AvailabilitySlot`, since these shapes are consumed by client components. `volunteerForSubRequest` rejects the requester volunteering for their own request and rejects volunteering on a non-open request, upserts (not duplicates) on a repeat call from the same volunteer, and only notifies (a new `sub_volunteer` notification type, added to `NOTIFICATION_TYPES`/`NOTIFICATION_LABELS` so it flows through the existing preferences UI automatically) the requester and DM on a genuinely fresh volunteer, not on a message edit. `listVolunteers` restricts volunteer identities to the requester or DM — unlike the request itself, volunteer identities aren't broadcast to everyone browsing the pool. `setSubRequestStatus` only allows a single open -> filled/cancelled transition (no reopening), restricted to the requester or DM.

API: `GET`/`POST /api/campaigns/[id]/subs` (list is public, post requires sign-in + the access check above), `GET /api/subs` (the app-wide browsable pool, public, same reasoning as the existing public `GET /api/campaigns`), `POST`/`DELETE /api/subs/[id]/volunteer`, `GET /api/subs/[id]/volunteers`, `POST /api/subs/[id]/status`.

UI: `components/SubRequestPanel.tsx` (client, self-fetching like `RatingsPanel`) sits on the campaign detail page — a post form for the DM/approved members, a list of that campaign's own requests (open and past), and for the requester/DM a "view volunteers" toggle plus "mark filled"/"cancel" buttons. A new `/subs` page (server component, matching `/campaigns`'s pattern of server-rendering the list and delegating just the interactive sliver to a small client component) is the app-wide browsable pool, with a new `components/SubVolunteerAction.tsx` handling the volunteer/withdraw control per row. Added a "Find a sub" link to the NavBar.

Judgment call, explicitly flagged: "mark filled" is just a status flag the requester/DM sets after reading the volunteer list and deciding off-system who's actually subbing in — there's no seat assignment, no character handoff, and no guardrail enforcement in this phase. That's phase 2/3 territory per the backlog's own scoping, deferred exactly as planned.

Testing: new `__tests__/unit/subRequests.test.ts` (14 tests) covering: posting defaulting to open (DM and player); rejecting a post from someone with no relationship to the campaign; rejecting an over-length note; open requests appearing in the app-wide pool and disappearing once filled while still visible on the campaign's own list; listings enriched with campaign/requester context and volunteer counts; a stranger volunteering with `viewerHasVolunteered` correctly reflecting per-viewer state; rejecting the requester volunteering for their own request; rejecting volunteering on a non-open request; a second volunteer call updating the message instead of duplicating; withdrawing a volunteer; restricting the volunteer list to the requester/DM; the `sub_volunteer` notification firing once each to the requester and DM on a fresh volunteer but not on a message update; the requester/DM marking a request filled only from open (and a second status change on an already-terminal request being rejected); and rejecting a status change from someone who isn't the requester or DM. 160 -> 174 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server (fresh SQLite db, spare port) drove the full flow end-to-end via independent per-role cookie jars: a stranger's attempt to post a sub request 403'd; the approved player posted one; the app-wide pool showed it (including while signed out); an unrelated signed-up user volunteered with a message; the requester attempting to volunteer for their own request 403'd; a stranger attempting to view the volunteer list 403'd; the requester successfully viewed it; the DM marked the request filled; a second status-change attempt on the now-terminal request 403'd; the app-wide pool no longer showed it while the campaign's own list still did (correctly showing "filled"); and restarting the server against the same SQLite file confirmed the data persisted correctly.

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #9, shared Section wrapper for the campaign page)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. With both #20 and #27 now at their backlog-endorsed "phase 1" stopping points, going further into either one's phase 2 (a three-way approval/character-custody state machine for #20, or timezone handling and a real search/matching query for #27) would mean attempting a piece explicitly flagged as needing its own dedicated scoping effort, not a natural continuation of tonight's pattern of small, complete slices. Picked #9 instead — a concrete, low-risk, already-scoped item sitting in this file's own "worth revisiting" notes, and one that had only gotten more relevant: the campaign detail page had nine feature panels by the time this was picked up (DmControls, RequestsPanel, RosterPanel, ScheduleForm, PartyNotesPanel, SessionLogPanel, RatingsPanel, SubRequestPanel, and the page's own characters block), each repeating the identical `rounded border border-black/10 p-4 dark:border-white/10` outer-card className verbatim.

Extracted `components/Section.tsx`, a thin wrapper around exactly that repeated string, and switched all nine call sites to use it. Deliberately scoped narrow: `Section` only owns the outer card styling, not each panel's header markup — several panels have a header with an inline action (the characters block's "Manage your characters" link, RequestsPanel's approve/decline buttons sitting elsewhere), and forcing every panel through a single header API would have meant either losing that flexibility or adding escape-hatch props that don't earn their complexity for a pure style extraction. Every panel's internal JSX is otherwise untouched.

No behavior or visual change anywhere — verified by the full 174-test suite staying green without any test edits (notably including the HTTP integration test that specifically checks rendered HTML content across every access role: DM, approved member, pending requester, left member, stranger, signed-out visitor — a change that broke any panel's rendering would have shown up there), plus a manual smoke test rendering the campaign page as both a signed-out visitor and the DM and counting occurrences of the shared card class in the raw HTML to confirm every expected section still renders.

No changes to email/SMTP (#6c) or the deployment decision (#3).

2026-09-06 (live session, continued: backlog #20 phase 2, owner/DM approval + temporary custody)
Continuing the owner's "keep iterating through the backlog for the rest of the day" instruction. The owner explicitly asked to scope backlog #20's phase 2 next (rather than pick a different item), so this entry documents both the design decisions made before writing any code and the implementation itself.

Before implementing, three structural questions were put to the owner rather than guessed at, since this is explicitly "the heaviest single item" in the backlog and getting the shape wrong would mean throwaway work: (1) how strict should "the table" be as the third approval vote — the answer was owner + DM required, the rest of the active party notified but not a blocking gate, which turns "three-way approval" into a bounded two-approver state machine; (2) should the workflow require a request to name a specific character — yes, since there needs to be an "owner" and something to hand temporary custody of; (3) build the full (two-approver) workflow in one pass rather than slicing further — yes. These three answers shaped everything below.

Data model: `sub_requests` gained a nullable `character_id` (set at creation, validated to be a character the requester owns AND currently linked to this campaign — this is what lets "the requester" and "the character owner" be treated as the same person everywhere downstream, with no separate owner concept needed). New `sub_placements` table (request_id, volunteer_id, guardrails_note, owner_approved, dm_approved, status: pending/confirmed/declined/cancelled). `characters` gained a nullable `temp_pilot_user_id` — a pure display marker ("currently piloted by X"), set when a placement is confirmed, cleared by an explicit "end sub" action (this app has no session-duration tracking to clear it automatically).

Backend (`lib/subPlacements.ts`): `createPlacement` is restricted to the requester or DM, only for a character-linked open request, only for someone who's actually in that request's volunteer pool, and only one pending placement per request at a time. `ownerReview`/`dmReview` each independently approve or decline; either one declining ends the placement immediately without waiting on the other (no reason to make someone wait on a review that's already moot). Once both approve (in either order — `resolveIfBothApproved` runs after each review and checks both flags, not just the one that just changed), the placement is confirmed, `characters.temp_pilot_user_id` is set, and the parent `sub_requests.status` becomes `filled`. `cancelPlacement` lets the requester/DM back out of a still-pending placement outright (e.g. the volunteer changed their mind). `endSub` clears the custody marker and is restricted to the character's owner or the campaign's DM; the confirmed placement record itself is left alone as history, only the character's live state changes. A new `sub_placement_pending`/`sub_placement_resolved` notification pair (added to `NOTIFICATION_TYPES`, flowing through the existing preferences UI automatically) notifies whichever of {owner, DM} didn't act plus the volunteer on creation, and notifies the volunteer + owner + DM + every other currently-active party member on any resolution (confirmed/declined/cancelled) — this is exactly what "the table is informational" means in practice: everyone finds out, but nobody's presence is a gate.

API: `POST /api/campaigns/[id]/subs` now accepts an optional `characterId`. New routes: `GET`/`POST /api/subs/[id]/placements` (list is public/informational like the request itself; create is access-checked), `POST /api/subs/placements/[placementId]/owner-review`, `POST /api/subs/placements/[placementId]/dm-review`, `POST /api/subs/placements/[placementId]/cancel`, `POST /api/characters/[id]/end-sub`.

UI: `SubRequestPanel` gained a character picker on the post form (only shown if the poster has characters linked to this campaign), a "Select for approval" button next to each volunteer in the owner/DM's volunteer list, and a placement card showing both approval states, an editable guardrails textarea for the owner, approve/decline buttons for whichever reviewer is viewing, and a cancel option. `CharacterSummary` gained a `pilotName`/`canEndSub` prop pair rendering the "Currently piloted by" badge; ending the sub is a dedicated new `components/EndSubButton.tsx` client component rather than a callback prop, because `CharacterSummary` is rendered directly by Server Components (the campaign page, the public player page) that can't pass event-handler functions across the server/client boundary — only a genuine Client Component can own that click handler. `/api/characters` was extended to resolve and include `pilotName` alongside the existing `campaignTitle` enrichment, following that route's own established pattern.

Judgment call, explicitly flagged: guardrails are a free-text note the owner attaches, not the structured "blocks permanent/irreversible actions by default unless explicitly allowed" checkbox system the backlog line originally sketched, and there is no programmatic enforcement of them at all — this app is a matchmaking/session-tracking tool with no in-game action to gate, so "enforcement" can only ever mean "the DM and volunteer can read what the owner asked for." Building a structured checkbox system for guardrails that are inherently unenforceable felt like complexity without a real payoff; a note serves the actual need (communicating the owner's wishes) just as well.

Testing: new `__tests__/unit/subPlacements.test.ts` (18 tests) covering the full state machine — placement creation by requester/DM, rejecting creation by a stranger/for a non-volunteer/for a characterless request, rejecting a second pending placement, confirming with custody transfer regardless of approval order, either reviewer's decline ending things immediately, rejecting a review from the wrong person or on an already-resolved placement, cancel-and-reopen, end-sub access control and its effect on the character record vs. the historical placement record, the resolved-notification fan-out to all four roles, and listing placements including resolved ones. Extended `__tests__/unit/subRequests.test.ts` with 4 tests for the new `characterId` parameter (accepting it, rejecting a character the requester doesn't own, rejecting one not linked to this campaign, defaulting to null). 174 → 196 tests, all green; build and lint both clean. Manual smoke test against a real `next start` server drove the complete flow end-to-end across four independent users (DM, requester/owner, volunteer, stranger): character-linked request creation, volunteering, a stranger blocked from creating a placement, a duplicate-placement attempt blocked, the DM blocked from giving the owner's review and vice versa, owner approval with a guardrails note, confirming that custody stayed unset with only one approval in, DM approval completing the confirmation (custody set, request marked filled), the "Currently piloted by" badge rendering correctly on both the public player page and the campaign page (including the DM's "End sub" control), and ending the sub correctly clearing the badge.

No changes to email/SMTP (#6c) or the deployment decision (#3).
